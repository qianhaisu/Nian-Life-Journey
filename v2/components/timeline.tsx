import type { LifeEvent, Media } from "@/lib/types";
import { LifeEventCard } from "./life-event-card";
export function Timeline({ events, media }: { events: LifeEvent[]; media: Media[] }) { return <div className="timeline">{events.map((event, index) => <section className="timeline-entry" key={event.id}><div className="timeline-date"><span>{new Date(`${event.occurredAt}T12:00:00`).getFullYear()}</span><strong>{new Intl.DateTimeFormat("zh-CN", { month: "2-digit", day: "2-digit" }).format(new Date(`${event.occurredAt}T12:00:00`))}</strong></div><div className="timeline-line" aria-hidden="true"><i /></div><div className="timeline-content"><span className="eyebrow">{event.eventType === "outing" ? "A day outside" : index === 0 ? "Most recent" : "A small memory"}</span><LifeEventCard event={event} mediaItem={media.find((item) => event.mediaIds.includes(item.id))} featured={index % 3 === 0} /></div></section>)}</div>; }
