# HEALTH-01-R2 · 按 Codex 审核返修 · 执行回执（脱敏）

> 2026-09-20。执行：Claude Code。审核：Codex（**尚未开始**）。
> 依据 [`health-tracking-batch-01-r1-codex-review-2026-09-20.md`](health-tracking-batch-01-r1-codex-review-2026-09-20.md) 的 F1–F6。
> 前两份回执（[首次](health-tracking-batch-01-report-2026-09-20.md)、[R1](health-tracking-batch-01-r1-report-2026-09-20.md)）保留为历史，未覆盖。
> 本文脱敏：不含病历原文、药物剂量数值、患者标识、家庭聊天原文、汇总稿正文与私有外链。
> **本文是执行者自检结果，不是审核结论，不是医学验证完成，医学分析未经任何医生审阅。**

| 项 | 值 |
|---|---|
| 基线 SHA | `ff1bfc6` |
| 交付 SHA | 见本次提交 |
| 前序交付 | `e32cee5`、`0c99b88`，均保留在历史中，未回退 |
| 仓库修改路径 | 本回执（新增）、`v2/scripts/health-audit/wechat-export-dedupe.mjs`、`v2/test/health-audit-wechat-dedupe.test.mjs` |
| R2 私有目录 | `C:\Users\teddy\NianlifeOps\health-tracking\2026-09-20\run-20260920-r2\` |
| 保留目录 | `run-20260920-a`、`run-20260920-r1`、`codex-review-r1` 均**原样未动** |

---

## F1 处方与诊断提取错误 —— 已修，并撤销一个假冲突

**修改**：把那份纸质病历用派生放大图逐药逐字段重新转写。结果：
一条口服抗菌药的规格与单次量此前被读错、一条抗过敏药的用法被串行粘连、**一条雾化药整条漏掉**、
一个疗程被挂到了错误的药上；另有一份检验报告的临床诊断此前被补全成完整词（原图本就截断），
以及一份过敏原报告的项目数被错算（其中第一项是总 IgE，不是过敏原）。

**证据**：更正后与同次就诊的电子处方逐项比对，**7 条药品、规格/单次量/途径/频次全部一致，0 处冲突**。

**因此撤销**：R1 报出的「同一药品两个单次量」**不是冲突，是我读错纸面**。
该条已从事实层、医学摘要和**家庭确认清单**中一并删除——不该让家里回答一个由提取错误制造的问题。

**一处与审核卡不一致，如实报告**：审核卡称某雾化药纸面频次与电子处方不同。
R2 将该行放大后重读，**纸面与电子处方一致**，未能确认存在该差异。已附派生图路径供复核。

**残余**：一条口服药在两份来源里分别写成量词与毫克数，数字不可自动换算，保留为待核对，不自行折算。

## F2 按分钟匹配冒充逐条审核 —— 已改为逐消息

**修改**：审核表的键改为「会话 + 时间 + 原文前缀」，由程序解析成**真实消息身份**
（结构化导出用平台消息 ID，Markdown 用文件行号）。**94 条登记全部解析到唯一消息，0 条未匹配。**

**纠正的实质错误**：同一分钟内的多条消息此前共用一个结论。拆开后发现其中**两条是提问**
（"要不要吃某药""晚上吃了吗"），R1 把它们记成了**已发生的用药**。现已改判为提问类。
纯媒体路径设为硬拦截：即使人工登记要求纳入也会被降级排除。
`抽中样本` 与 `已完成复核` 拆成两个独立字段，抽中不再自动等于已审。

## F3 覆盖与事实链 —— 部分完成，缺口如实报告

**先报一个计数更正**：R1 说「1,321 条未审核」。**这个数字是错的**——
R1 的媒体路径正则在写入脚本时被 shell heredoc 吃掉一个反斜杠，只匹配了一种路径分隔符，
于是大量「整条内容就是一个图片/视频文件名」的消息没被识别。

正确计数：2,077 条候选中 **1,040 条是纯媒体文件路径**（关键词命中的是文件名里的数字），
**实质候选 1,037 条**，其中**已人工逐条审核 94 条，仍未审核 574 条**。

**完成的**：
- 微信从候选处置表升级为**真正的事实层**，87 条，每条具备稳定事实 ID、来源定位、原文、
  说话人角色、时间精度、审核状态与病程/就诊关系。
- **补齐两段此前没有的病程**：其中一段此前只有单次就诊记录，本轮用家长在就诊当日写下的
  第一手病程自述 + 托班观察补齐了整段，并取得**该段的实际给药证据**与一条**「明确未用退热药」**的记录；
  另一段**全程没有任何医院原件**，微信是唯一一手来源。
- **排除一处危险误读**：某条托班消息里的「39 度左右」经上下文核对是**药浴水温**，不是体温。
- 汇总稿**已作为正式来源行进入来源清单**（含哈希、声明覆盖范围、上游可访问性），
  不再只把哈希藏在对账文件的表头里。
- 审核指出的一条「把未定日的早期发热锁定到某次已知病程」已**降级为候选关联**，该问题重新打开。

**残余**：574 条未逐条阅读。已按月列出；其中原件完全空白的两个时段占 254 条，是下一步首选。
**没有把未做的阅读转交第二批**——它们留在本批的未完成项里。

## F4 去重仍有丢版本路径 —— 已修，并用审核方自己的探针验证

- 同一份导出内同键不同内容此前被 Map 覆盖 → 索引改为「键 → 版本数组」，
  任一侧缺版本即判为内容分歧、不可证明包含。
- 同一会话的 Markdown 此前一律按格式副本压制 → 改为**只有能证明被结构化导出完整包含才降级**，
  否则标为「格式变体未验证」并保留，绝不因为格式就丢内容。

跑审核方提供的复现探针：两项均已反转为期望值。仓库合成测试 **19 → 22 个**，新增 3 个防丢失用例。

## F5 验证假通过 —— 每个检查器都配了反例

**做法**：把数据故意改坏后检查器必须失败。**10 条反例全部生效**：
篡改原件哈希、回退一条已生效的更正、指向不存在的消息 ID、上下文窗口为零、引用文件不存在、
重复的规范事实、把预约算成就诊、事实计数漂移、汇总稿哈希漂移、谎称上游可访问。

同时：引用检查现在**真的去文件里查**（结构化导出按消息 ID 查找，Markdown 校验该行确实是一条消息起始）；
新增来源通过注入点**真正进入事实生成**后再验证既有 ID 稳定性（不再是空跑）；
计数检查改为实算而非恒真。验证在 R2 目录内执行，未改写任何已交付产物。

## F6 医学证据与行动 —— 逐条修订

1. **删除**「任何按体重折算都用某个历史体重」。两个历史测量各自保留；
   **涉及当前用药需要当前体重**，已列入家庭问题。既往处方回顾与当前用药判断分开成节。
2. 剂量结论改写为「与一份说明书条目的区间比较**未发现超出**」，不再写成「已确认正确」，
   并注明未核对实际厂家规格。复方制剂重复成分的依据改为**通用名中的成分名**，并注明以药盒为准。
3. **消除表述矛盾**：行动清单第 0 节改为**三档**（立刻急诊 / 当天当面看 / 居家观察），
   条目来自已读到正文的指南，不再一边说「只能拿来问医生」一边又照它安排就医。
4. 发热时长改为「**满 5 天**」（原写「超过 5 天」会延到第 6 天），并正确归入**中危**而非紧急。
5. 感染次数改为**按病程计**，不再把同一段病程的连续诊断加成多次；
   并写明计数**可能偏低也可能偏高**，**删除**「真实次数只可能不少于现计数」这种单向断言。
6. **撤回**一条依赖未读条款得出的倾向性判断（某年龄段用药首选档）；
   嗜酸粒细胞升高改为**并列多种可能解释**，补上「正常炎症指标不能独立排除感染」。
7. 一类上呼吸道感染的细菌性判断依据，由「只有症状超过 10 天」一种门槛补为**三种表现模式**；
   并明确**所读网页日期不等于指南版本**（页面为近期，指南本体仍是早年版本）。
8. 当前追问由 4 条扩为 6 条（补喝水/尿量、当前体重），并改为「**起点，不是全部**」；
   出现危险信号时按分档立即行动，不等这份清单。

**证据等级变化**：两份指南由「仅检索摘要」升为**已读正文**（其中一份经官方收录镜像读取，直站仍被拒）。
现为：正文已读 2、二手逐字引用 2、**仅检索摘要 3**。按 Teddy 指示，**未为年度版本差异额外耗时**，
已如实记录所用版本。

---

## 检查结果（命令与退出码）

| 命令 | 退出码 |
|---|---|
| `npx eslint scripts/health-audit/ test/health-audit-wechat-dedupe.test.mjs` | 0 |
| `npm run typecheck` | 0 |
| `node --import tsx --test test/health-audit-wechat-dedupe.test.mjs` | 0（22/22） |
| `npm test`（全仓） | 0（**1,475 项，1,464 通过，0 失败，11 跳过**） |
| `npm run build`（生产构建） | **0** |
| 私有 `r2_validate.py` | V1–V10 全 PASS，**反例 10/10** |

R1 回执未提供生产构建证据，本轮已补。

---

## 外部服务与数据流向（按审核要求单独披露）

- 微信候选的主体预分类由既有脚本生成，该脚本**会把候选消息与上下文提交到已配置的 DeepSeek API**。
  因此准确表述是「**不进 Git**」，**不是「未离开本机」**。R1 回执的措辞可能引起误解，在此更正。
- 授权依据：项目 `CLAUDE.md` 的长期规则（图像/内容识别交由该服务的指定模型）
  与「外部 API 费用不再需要确认（2026-09-12）」。
- 调用量：R1 一次性分类 2,077 条候选（每条 1 次请求，另有 3 次能力门探针）；
  医院图 OCR 亦在 R1 完成。**R2 未重新调用该服务**，直接复用 R1 产物。
- 费用：脚本未记录单价，**无法给出金额**，只能提供请求计数。
- **R2 本轮没有向任何外部模型服务发送数据**；新增的仅公开医学资料检索，使用去标识化临床问法，
  未提交姓名、就诊标识、原文或图片。

---

## 未验证项

1. **574 条微信候选未逐条人工审核**——最大缺口，已按月列出。
2. 实际用药仍大部分未知（两段病程共 7 条给药证据，其余未确认）。
3. 6 张无日期病历的日期为推定。
4. 汇总稿上游资料**仍全部未提供**，无法核对其转述是否忠实。
5. 3 条医学证据仍只到检索摘要级。
6. 35 张图由单人核对，**未经第二人复核、未经医生复核**。
7. 2026-09-19 之后无任何记录。

## 阻塞

无硬阻塞。

## 下一步

**提交 Codex 审核，状态「已提交审核」。未进入 HEALTH-02。**

## 哪里需要 Teddy

需要家庭回答的共 **6 条**，均在私有行动清单第 1 节，按类别是：
最近一次复诊之后的症状变化、药实际哪天停的、这几天有无发热及已烧几天、精神进食睡眠呼吸情况、
喝水与尿量、**最近一次体重及测量日期与地点**（当前用药折算需要当前体重，两个历史值都不能用）。

**已删除**一条由提取错误制造的问题（某药两个单次量）——那本不该问。

另有 3 条只影响档案准确性（一次体重测量的日期地点、一段历史摘要记的抗菌药名与原件不符、
一段用药记录的年份存疑），以及 1 条可选补图（就诊列表更早区段）——**不以补图阻塞当前工作**。

## 边界遵守声明

未连接生产或 Neon、未写业务数据库、未跑迁移、未启动导入 worker、未改页面、未部署、
未回退 SHA、未新建分支、未强推、未改写含真实群标识的旧历史、未修改其他会话文件与审核目录。
原始资料只读，运行前后哈希一致。
健康原文、原图、派生放大图、患者标识、家庭消息与汇总稿全文**全部留在私有目录，未进 Git**。
