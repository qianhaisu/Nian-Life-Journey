# 首页仅保留跨日回忆 — 2026-09-19

用户明确要求 Codex 直接实现并发布，无需再次确认。

首页停止生成 day 候选，并在数量限制前按最终幻灯片的拍摄日期过滤所有候选，只保留至少两个不同日期的回忆。主题、季节、最近一周继续展示。原始照片、故事、审核和可见性数据未改动。

验证：类型检查、Lint、生产构建通过；全量测试 1360 项，1349 通过、11 跳过、0 失败。新增单日故事和单日主题均不能出现在首页的回归用例。

发布 SHA：7a5c14b141170afc66291d093b6e10abc2a0db58。
生产地址：https://nianlife.cn/
回滚容器：nianlife-diag-web-pre-7a5c14b-20260919-094413（上一镜像 9cae65e）。
沿用上一生产配置，MONTH_CONTENT_DIR 和只读挂载保留，Organizer worker 仍关闭。

线上 health 返回相同 SHA、数据库连接正常；首页 HTTP 200。Chromium 实际页面收到 16 段回忆，全部跨日（每段 5–20 个不同日期），无 day 类型。“换一段”、照片加载、390px 无横向溢出通过；390×844 和 1280×800 截图保存在本机临时目录，不进入仓库。未实测 iOS 真机。
