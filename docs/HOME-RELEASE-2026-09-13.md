# HOME-RELEASE · 最后发布执行单

Codex 收口决定：用户要求尽快结束首页改造、不再追加设计。接收者为现有页面 Claude Code；数据 Code 不再独立开启工作。

## 审核结论

- 发布代码目标：`00099a167266b8bc72f91c9c5bfc36134a4ac1d3`（00099a1），不是旧版 713d550，不盲目以 HEAD 代替。
- Codex 已看 relayout 的桌面完整页面、390 和 320 截图，并核对提交范围；桌面与 390 的照片、短题签、轻便签方向认可，视觉修改终止。
- 功能操作与 typecheck/lint/1094 pass、10 skip/生产构建依据页面 Code 已报告的验证；Codex 本轮未独立完成实时点击，不混写证据。
- 唯一发布前补证：`relayout-320x700.jpg` 中照片区域为空。不能仅凭框尺寸算照片已加载，也不能据此断言生产有缺陷。页面 Code 在现有验收服务的真实 320 视口，确认图片加载完成（complete 且 naturalWidth > 0）并补一张实际含照片的截图。若只是拍得太早，不修改代码、不重跑全套检查；若真实加载失败，只修该故障，跑受影响检查并记录替代 SHA。
- **上述加载确认成功后，直接执行下述私有发布，不再等待 Codex 或用户的另一轮审批。** 这是现有 standing 授权范围内的具体放行，替代 RELAYOUT 文档的发布暂停。

## 页面 Code 连续执行至完成

1. 使用现有发布流程，打包上述已提交代码，记下当前 ECS 镜像/部署 SHA 作为实际回滚点，保留现有 RDS/OSS/运行配置与回环访问方式。不要重建环境、改 DNS、开公网或启动 Organizer。
2. 0016 已正式应用 RDS，**不再跑迁移、重建表或写入合成验证数据**。本轮不重跑导入、审图、模型能力探测或 Git 敏感内容排查。
3. 部署后通过现有私有入口 `http://127.0.0.1:18080/` 核对实际部署 SHA；使用独立验收浏览器 `?habitReport=off` 做一次桌面/窄屏照片加载、换图带动题签/链接、便签展开的简短检查。保留用户日常浏览的上报设置，不把验收关闭开关变成服务器默认设置。
4. 有真实发布故障时按现有回滚点恢复并回报；没有故障就停止检查，不为优化继续改代码。不新建服务/分支/session。
5. 在自己的 STATUS 顶部写最终状态，修正旧“版式进行中”和旧发布 SHA，记录部署 SHA、回滚点、私有入口、补证/线上证据路径和结果。只提交自己的文档并 push main；不把文档 SHA 冒充运行代码 SHA。
6. 回执限简短：`已部署/失败回滚`、实际代码 SHA、验收地址、关键复验结果、唯一剩余限制。完成即结束本轮，不再列首页范围外的原则六/七作为欠账。

真实 AI 视觉评分仍未交付，当前为确定性降级；作为单独剩余能力如实记录，不称“AI 全部完成”，也不因此阻塞本次已认可的首页发布。

本执行单写入不代表 Claude 会话已被唤醒，也不代表部署已经完成；以执行者 ACK 和实际发布回执为准。仅页面 Code 执行，不要求用户再次确认技术方案。
