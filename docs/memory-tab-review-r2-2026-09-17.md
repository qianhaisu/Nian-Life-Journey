# MEMORY-01/02-R2 审核：保留已完成项，定点修正剩余问题

日期：2026-09-17。基线：`main @ 93a68b6`。

本轮检查：本机 R2 报告、HTML、独立核对页、选片记录、保存的模型响应文本、两张实际页面截图及导入器相关代码。未重新查询生产数据库/接口，未调用模型，未运行浏览器交互，未修改业务代码、样稿或生产数据。生产计数及调用次数仍标明来自执行者报告。

## 结论

- MEMORY-01：诊断主方向可以收口作为后续实施依据；报告与核对页的数字解释、证据附件仍需同步。照片完整层已更新不代表故事/月总结链路恢复。
- MEMORY-02：`changes_requested`。实际交互和页面结构有进展，但封面有可见缺陷，视觉分析与来源审核证据仍不足以宣称全部通过。
- 不要求推倒 R2，也不扩大到全月重审。只做本文件列出的局部修正；不得把样稿生成或本次审核解释成设计批准、生产发布或 MEMORY-03 全月执行授权。

## 已完成项保留

1. 月页确有原位 `<details>`，默认 2 张、展开追加 4 张，无需跳转。
2. 9/1 Event 有 6 个实际图片元素，默认展示，已有截图，不再用 ID 列表代替图。
3. 三份 Event 有可见主标题，资料统一展开的结构已存在。
4. 响应式裁切规则确实写进 CSS；未获得桌面视口也如实披露。规则生效与构图合格是两项检查。
5. 未知发言人使用稳定、可区分的标记；不应继续猜测私人姓名/关系。原有权限拦截不应绕过。
6. MEMORY-01 区分了照片、故事、总结的新鲜度，并补了单一 source 的链路文字记录。

## 1. 封面：现有截图就是未通过的证据

查看 `02-sample/screenshots-r2/01-month-card-w501-mobile-crop-verified.jpg`：下边界穿过嘴部，嘴和下巴未完整入框。R2 报告“下巴完整、无裁切问题”与截图相反。

R2 还改变了卡片宽度，不能将旧尺寸下的 Sharp 模拟结论直接搬来。修正时记录实际图片容器宽高、图片宽高与 object-position，按最终画框重新调焦点/高度或选择有效替代图。验收目标是完整的表情与必要动作，不是必须保留 19% 或 45%。

现有 501px 视口即可修复和验证这一处。390px 与桌面真实渲染仍单列待验证；不要因为工具无法扩窗而反复试 resizeTo/zoom，也不要据此称所有手机尺寸已经通过。

## 2. DeepSeek：截断的分析不能当作完整结果

`sep01-selection/SELECTION-LOG.md` 明确写两次分析都在生成中被截断。保存的 `sep01-deepseek-response-groupsABCEF.txt` 是未完成的推演文本，末尾停在半句；不是包含每图最终判定的完整结果。另一次响应没有独立原始文件，日志称有完整摘录，但现有文件主要是整理后的表格。

因此当前可以记录“执行者报告调用过 Flash”，不能据此验收“21 张均完成检测、模型响应校验及完整选片已通过”。仅有单色能力探测也不能替代多图编号对应验证。

修正要求：

- 先找回已有合法调用回执和完整最终响应；能复用的不用重跑。仅补失败/截断或缺失的结果，范围限原 21 张样本，不扩到全月。
- Flash 返回必须有完整最终结果并通过解析及逐图覆盖检查。保存脱敏的请求标识、实际请求/返回模型、输入图片 ID/checksum/编号映射、结束原因、usage 和最终结果。缺字段记未返回，不补造。
- 将截断、编号错配、遗漏图片、解析失败标为未完成，不能消费推演中的临时候选当最终审核结果。按既定预算/重试/停止规则处理，不静默切模型。
- 跨组场景动作比较和选片建议仍由 DeepSeek 负责。Claude 可以依据其结构化结果做编辑取舍并记录理由，不能把视觉比较改称“编辑判断”后自行批量完成。
- 核对实际文件：日志 A 组推荐 `sep01-h-groupA-2.jpg`，最终清单却采用 `sep01-a-0036.jpg`；需明确是编辑调整还是 ID/文件错配。分组表列出 9 组，核对页却写 8 组；报告“去掉 2 个”与最终减 3 张也应统一。
- 最终清单的时间与前面的分组时间有差异，尤其最后一张；须以同一 media ID 的字段语义和文件校验对应，不机械加 8 小时。

## 3. 发言人：核对账号与证明现实角色分开

本机代码确实包含所用的哈希生成公式。但 R2 方法写的是匹配 senderDigest 前缀，核对页也只给前 8 位；现有交付不是完整逐 source 身份映射证据。

对已有候选，补完整摘要一致性及对应的稳定 source/原始消息定位，记录姓名证据来自哪一条已有记录。无需继续枚举新名字，也不要绕过执行者报告的 PII 限制。若因权限或缺少来源无法补齐，就标待核实，不强行凑齐。

显示名匹配不能单独推出“园长”“某方长辈”等角色；没有明确来源的角色删去，仅保留有证据的显示名。班级账号发消息，也不等于确认某一位现实老师亲自发言。当前 9/1 正文使用“班级账号在群里说”的归属方式可以保留，相关报告不得再扩大为所有现实身份已确认。

“8 个已匹配发送者标识”与“8 个不同真实人物”也不同，已有映射中包含同一人的两种名称。未知 A–E 可保留，不构成要求再次猜测的任务。

## 4. 文案与去重：只做必要修正

- 月总结仍是“从跟着‘粥粥’发音，到自己说出……”，未核实的跟读机制并没有因替换“学舌”而消失；报告“9/2、9/7×2、9/8 是四个独立日期”也不成立。删去跟读/阶段推断，保留能证实的语言主题；月总结应比封面一句话提供更多归纳，不能仅换标点复述。
- 9/5 将“伸手想摸”换成“伸手碰了碰”，若依据仍只有一张视频封面，接触动作也未必得到证明。用现有合格解析/实际帧证据确认，否则收窄为静态可见描述。
- 9/8 仍称四组长度为 6+5+2+7=20、存在跨组 ID 重叠、并集又为 20，这三个命题不能同时成立。交本机逐事件来源 ID 列表与程序算出的各长度/并集；不再靠文字重新解释算术。
- 判断消息重复还需附件和引用关系证据。相同会话、发送者、秒级时间、正文只足以形成候选，不能证明“引用块和正文重复入库”的解析器根因。证据不足则保留两条来源，不能悄悄丢失资料。
- 9/7、9/8 Event 页底部仍有发言人哈希核对和数量审核说明，故“工程说明全部移走”尚未完成。将其真正移到独立核对页，清除失效的“见下方核对”文字。

## 5. 诊断收尾不另开大任务

`verification-r2.html` 第五节仍称 64 与 12 是同日不同筛选集合；MEMORY-01-R2 则称 64 是另一日计数被错标。同步为经核实的一种解释，不保留互相矛盾的两版。

稳定来源追踪目前在报告内有明确 ID；补已有只读探针/结果的准确本机路径，找不到就说明尚未留存，不能只写“已记录可复现”。不为补附件重做全量诊断。

时区报告提供的同源对照支持样本显示存在偏差；不外推全部存储都正确。23 点多的记录不能单独证明 UTC/上海跨日期处理正确，后续 MEMORY-06 用上海 00:00–08:00（UTC 前一日）的记录验证日期与时间；不必因这一测试尚未做而重跑整个诊断。

## 给 Claude 的定点收尾提示词

```text
执行 MEMORY-02-R2.1 定点修正及 MEMORY-01 证据同步，先读 CLAUDE.md 和本文件。
保留 R0/R1/R2，新增 R2.1 文件；保持本机样稿范围，不改生产/审核状态/信任名单，
不运行 Organizer，不部署，不推进全月处理，不新建分支。

保留已完成的原位展开、6 图默认展示、Event 标题和统一资料入口，不重做结构。
逐项落实本文件 1–5 节：
1. 先修封面，实际截图必须完整显示嘴和下巴；记录真实容器尺寸，不能照抄旧百分比。
2. 找回模型回执；仅补原样本中截断/缺失的 Flash 最终结果。校验完整输出、模型、
   多图编号映射与覆盖率。跨组视觉比较仍交 Flash，Claude 编辑取舍须有结果依据。
3. 核对最终图片文件/ID/校验值/时间与推荐，统一候选、分组、代表、最终和渲染数量。
4. 已有姓名候选只补完整匹配和来源证据；不再猜姓名或关系、不绕过权限拦截。
   不明角色删去或标待核实，账号与现实身份分开，不要求补齐全部未知人。
5. 删除月总结中未核实的跟读推断，核对静态图不能证明的接触动作；程序核算9/8
   来源并集并检查附件/引用，未知重复保留；清掉产品页剩余审核说明。
6. 同步诊断报告与独立核对页的64/12解释，补准确的已有证据文件位置。

输出本机 MEMORY-02-R2.1-report.md、相应样稿、核对页、截图和机器可核对清单；
诊断只补短勘误。每项写清“修正了什么、证据在哪、什么未验证”。
桌面/390px仍做不到就如实保留待验证，不反复尝试相同扩窗方法，不宣称全端通过。
仓库只提交脱敏短回执，检查后push当前main，保留其他会话修改。
完成后供Codex复核；不把文件齐全等同于验收通过。
```
