# MEMORY-02-R2.1 审核：已完成项保留，补齐三处可复现缺口

日期：2026-09-18。基线：`main @ c431aab`。

审核范围：本机报告、HTML、两份模型 JSON、实际图片文件和两张封面截图；程序复算图片覆盖数、文件字节对应和已有姓名声明的哈希公式。不调用模型，不访问生产库，不改样稿/媒体/审核状态，不部署。

本机机器核对结果：`C:\Users\teddy\NianlifeOps\memory-tab-20260917\codex-review-r21\artifact-audit-20260918.json`。该文件含私有映射，仅留本机；仓库只保存本脱敏审核。

## 结论

R2.1 不必整体重做。封面局部裁切、18 张图片的完整分析、三处代表图替换及既有页面交互可以保留。但“21/21 已完成”和“全部采用本次代表图”仍与实际文件不一致，发言人证据还发现一处可复算不一致，因此尚不能给整体验收通过。

### 已核实的进展

- 新封面截图能完整显示嘴和下巴；桌面尺寸容器截图也保留完整表情。接受这两个画框的局部裁切，不视为 390px/1280px 整页验收。
- 调用记录列出 6 次 `deepseek-flash → deepseek-flash`、`end_turn`；保存文本中能解析到逐图结果，原有截断问题在这些组中已修复。
- 页面引用的 6 张最终图片文件全部存在。A、户外、鱼缸、卡片四组的最终文件，按字节比较均对应本次推荐。
- 月页原位展开、Event 可见标题和六图默认呈现继续保留。
- 角色标注收窄、64/12 解释同步，以及未知身份不再继续猜测的处理方向保留。

## 必须补齐的三项

### 1. 实际覆盖 18 张，另 3 张仍缺完整证据

`deepseek-r21-parsed-summary.json` 只有六组：A=3、C=2、户外=3、鱼缸=4、沙发=2、卡片=4，合计 **18 张**，唯一文件数也是 18。完整结果文件的组列表相同。

B/E/F 三个单张组不需要组内比较，但仍需要截图分类及画面描述。它们不在这次完整结果中，先前对应响应又已被确认截断；当前交付没有提供三张可复用的完整独立结果。因此不能把“单张无需比较”写成“21 张均已检测”。B 组还用于最终页面。

只补这 3 张的 Flash 完整结果，或提供已有且确实完整的有效缓存；不重跑已通过的 18 张。合并后按输入清单检查唯一图片覆盖确为 21，并明确哪些组完成了组内比较。跨组比较/编辑依据分别记录，不将二者混为同一项。

### 2. 沙发组最终仍是旧图

本次 JSON 的沙发组推荐为 `sep01-r-groupD-09.jpg`。页面引用的 `sep01-final/05-sofa.jpg` 按字节比较实际等于 `sep01-q-groupD-08.jpg`，不是本次推荐文件。

按既定方案改为本次推荐图，或明确记录有证据的编辑调整；不能继续写“全部采用本次推荐”而保留不一致。最终清单、实际文件和月页/Event 引用须由程序一并检查。仅修改这一处，不重新替换其余已对齐的图。

### 3. 已有姓名表仍不是完整匹配证据，且一行计算不一致

核对页称“完整前16位”，实际存在 15 位、16 位和省略号；前缀不是完整 SHA-256。对其中 6 行已有明确姓名、无省略号的声明按项目公式复算，5 行前缀匹配，**第 3 行不匹配**。本机审核 JSON 已记录该行和计算结果。

这是表中声明的可复现不一致，不足以判断具体是哪一环节错了，也不能直接反推另一姓名。只核对已声明的名字及其已有 source 原始摘要，保留源值、完整计算值、是否相等与来源定位。若是抄写错误则更正；缺原始依据则标待核实。不要从当前前缀补写剩余字符来伪造匹配，不枚举未知人名，不绕过权限拦截。

未知 A–E 不需要补齐；姓名字符串、账号与现实角色继续分开。无需因这一项重开全员身份调查。

## 已知限制与后续归属

- R2 HTML 被覆盖，且共享 `sep01-final/` 的部分文件也已替换，旧页面不能再作为独立可复现快照。承认失误是正确的，审核文字不能替代原版资产。后续先复制当前版本及其引用资产，再编辑新版本；无真实备份就不伪称恢复旧版。
- 390px/1280px 整页、上海跨日时区窗口按已有计划保留待验，不因本次局部修正无限重复扩窗尝试。
- 9/8 的“总和20、并集20、零重叠”在算术上已自洽，但目前报告主要是结果文字；不同 importBatchId 本身不证明两条消息等价，后续来源核对仍需附件/引用证据和逐对 ID 映射。保留原始来源，不由本次审核自动授权写入合并。
- 月总结目前是词语事实清单，已消除跟读推断，但还不是完整的月度概括。文案效果应与完整月份内容一起审核，不把“没有编造”当成“总结质量已通过”。本次不再为此重做整套样稿。
- MEMORY-01 的实施方向继续保留：照片完整层新鲜度、默认阅读、故事和月总结是不同层；持续整理和时区问题仍属于原计划，不能因局部样稿通过而称生产已修复。

## 给 Claude Code 的补齐提示词

```text
执行 R2.1 的小范围补齐，先读本文件及当前 CLAUDE.md。
不整体重做、不处理全月、不改生产/审核状态、不运行 Organizer、不部署。

1. 先冻结当前 R2.1 HTML、报告及引用资产清单，复制到独立的新版本目录后再编辑。
   新版本使用自己的媒体文件，不能再改共享图片导致旧版一起变化；记录真实备份边界。
2. 只补 B/E/F 三张单图的 Flash 分类/描述完整结果，或给出有效完整缓存。
   保留18张已完成结果，校验合并后的唯一覆盖为21，保存实际模型/结束原因/用量。
3. 对齐沙发组：本次推荐r，实际页面仍是q。修正文件与引用或披露有依据的调整，
   用字节/checksum核对最终六图清单、月页首屏与展开、Event引用一致。
4. 对本机 artifact-audit-20260918.json 指出的姓名表第3行做已知候选复核，
   提供已有source的完整摘要与完整计算值；其余声明也用完整比较结果留证。
   不猜未知姓名/关系、不绕过拦截；缺证据明确待核实，不能补造哈希字符。
5. 报告明确哪些已经通过、哪些仍待验证。覆盖数和代表图对应由程序生成，
   不再手写推算。新截图标明实际版本/视口，旧截图不能冒充最终新版本整页。

私人文件只留NianlifeOps。仓库仅提交脱敏短回执，检查后push当前main，
不新建分支，保护其他会话修改。完成供Codex复核及Teddy看样，不自动推进MEMORY-03。
```
