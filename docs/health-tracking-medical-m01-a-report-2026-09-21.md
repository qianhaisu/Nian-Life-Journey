# HEALTH-M01-A 回执（脱敏）

**状态：HEALTH-M01-A 已提交审核。** 这是执行方自检；真实分析包的内容审核由 Codex 独立给出，本回执不宣称通过，也不是医生审阅。

- 基线 main `3d28166`。交付 SHA 见提交记录。
- 沿用 HEALTH-04 r5 页面，没有新增栏目或依据按钮；没有连库/RDS，没有安装 PostgreSQL，没有部署，没有调用外部模型 API，没有建定时任务，没有重做历史全量阅读。旧产物与其他会话改动未动。
- 私有目录：`C:/Users/teddy/NianlifeOps/health-tracking/2026-09-21/health-m01-a/`（真实分析、证据登记、影响清单、快照均在此，不进 Git）。

## A. 病程总结

- 覆盖：账本登记的 **10 个病程**全部有分析版本（最近一段 EP-E 起，依次 F、D3、G、D2、D1、C、B、H、A）。EP-D3 与 EP-F 是账本已确认的同一病程两个视图，只写一份完整分析，D3 只指向它，不重复分析、不重复措施。EP-C（发热+呼吸道，与烫伤时间重叠）原被页面归入“烫伤”类，已改为只归呼吸道，烫伤类只有烫伤。
- 每版包含：发展特点、可支持的医学解释、不确定项、对观察护理/就医安排的实际影响、截至资料日期的状态；来源分四层（医生诊断/病历、家长报告、汇总稿转述、辅助推断）。资料不足处写明已能判断什么和为什么不能判断；结束未知的病程不写“已康复”（程序会拒绝这类措辞，包括否定式），历史状态明写“不代表现在”。
- 措施：只有带适用前提的条件性措施，共 10 条（观察与护理 2、就医安排 8，其中 3 条是“下次见医生时问”的问题），没有剂量，没有日期字段，不会成为预约或吃药提醒。多数已过去的病程没有单独措施，原因写在“对护理/就医的实际影响”里。
- 医学证据：11 条公开来源（NICE NG143/NG120/NG79、国家卫健委儿童社区获得性肺炎诊疗规范 2019、WHO 肺炎、NHS 烧伤/手足口病/支气管炎/鼻窦炎、2026 变应性鼻炎综述、WAO 5 岁以下喘息 2020），记录条款、适用人群、版本日期、链接和局限；事实引用（`factRefs`）与医学证据（`evidenceIds`）分开保存。公共检索只用通用医学词，不含孩子姓名或聊天原文。
- 证据阅读范围（如实）：NICE 三篇读了推荐章节全文；国家规范读的是**转载页**全文的诊断/评估/疗程章节（官方页对自动抓取返回 412，条款请对照官方文件）；NHS 页面读全文；WHO 读症状诊断段；综述读诊断部分；WAO 读诊断部分。**没有读到**：中国疱疹性咽峡炎/手足口病指南、变应性鼻炎 IgE 阴性的意义、嗜酸性粒细胞轻度升高的独立意义、幼儿烫伤瘢痕护理、复查胸片时机——这些都没有下结论，只作为“问医生”的问题。部分来源是英国指南或患者页面，已写明局限；WAO 为 2020 年版。
- 尚缺资料：孩子出生日期（月龄由“1 岁半”反推）；发现两处数据需核对——就诊材料里家长病史那条记录日期标为 7 月 9 日，早于文中的起病日；家长记录账本副本里只有一条“验证用合成记录”，分析未使用。

## B. 新资料 → 影响识别 → 新版分析 → 审核采用 → 页面更新

复用 HEALTH-02 的 `closureHash` 和 HEALTH-04 的失效判断，没有新建平行底账；分析版本是独立的追加式文件（`HEALTH_PAGE_ANALYSES`）。

| 步骤 | 命令 / 位置 | 自动还是人工 |
|---|---|---|
| 新手记、更正、就医材料、微信增量进入 | 既有入口（HEALTH-03 手记、`import`、`correct`）；微信原消息只是来源，健康事实抽取与本人/角色/上下文确认另做 | 抽取与确认：执行者人工 |
| 识别影响 | `baseline`（存基线，`--apply`）、`impact`（默认只预览，`--out` 存清单） | 自动 |
| 影响清单内容 | 受影响病程及原因、变更实体的版本与哈希、新来源、**未归属的本人记录**（列出邻近病程供人判断，不按日期接近强行归属，不延长红色区间）、被排除记录及理由；报告图只登记为材料，OCR 另批 | 自动 |
| 新版分析 | 执行者写正文 → `analysis draft`（追加版本，绑定依赖快照；同内容同快照重放不增殖）→ `submit` | 内容：执行者人工；入库：自动 |
| 审核采用 | `analysis adopt --by --basis`：记录采用人、时间、依据；**采用时再次比较依赖**，期间事实变了则拒绝（退出码 3），旧版保留；`reject` 退回 | 人工（Codex/审核人） |
| 页面更新 | 页面读取“已采用且依赖有效”的版本；采用后依赖又变则保留旧文字并标“待重新核对”，措施同步待核；无关病程不变 | 自动（下一次读取） |

生成或重新打指纹不会把旧文字变成已审核。已采用且依赖有效的版本，本身就是对该病程当前状态的一次核对，因此不再要求另外重打 `stamp-intervals` 指纹；依赖一变，指纹提示照旧生效。

## C. 检查

- 新增 `test/health-analysis.test.mjs`（5 组，合成数据）：生命周期与内容规则（草稿/待审不显示、采用后总结与两类措施出现、重放不增殖、剂量/日期/已康复被拒、无关病程不变）；更正让旧分析过期而保留文字、引用记录被更正也过期；审核期间事实变化拒绝采用、重起草后采用并取代旧版、退回不影响已采用版；影响识别（新观察/手记、非本人与提问被排除、离得远的只作独立节点、重放同一批影响清单不变、页面不因此画区间）；命令入口（默认预览不写、采用需 `--apply` 和依据、依赖变化退出码 3）。页面测试沿用 13 组，合并后 18/18 通过。
- 真实副本（只读，输入哈希前后一致，HEALTH-02 账本哈希 bbaeb49c… 未变）：10 版全部制作、提交为待审核，没有采用；影响预览在基线上无变化（退出码 0），加入那条合成家长手记后被识别为 1 条未归属线索（退出码 3）。
- 手机浏览器（390×844，预览副本，采用记录为“预览副本…未经 Codex 审核”）11/11：默认折叠不变、病程展开后出现总结与四层来源与不确定项、截至日期与“不是医生意见”、两类措施出现并带病程标签、无剂量、无横向溢出、未登录不含分析正文、分类修正。
- typecheck、lint 无问题；未连库生产构建成功（构建改动的 `next-env.d.ts`、`tsconfig.json` 已还原）。全仓 `npm test` 一次：1590 项，1576 通过、3 失败、11 跳过；3 个失败仍是 `test/editor-plan.test.mjs`（另一会话提交 `e509e17` 改了 `READY_LAG_DAYS` 而未同步测试），与本轮无关，单列、未改动。

## 局限与后续

- 真实分析包**未经 Codex 审核**，页面（真实配置）目前不显示这些分析；审核通过后按 README 命令采用。
- 医学解释来自公开材料与账本事实，不是医生诊断；措施是条件性建议，不是预约或用药提醒，不含剂量。
- 没有真实手机/读屏/深色模式、代理与 HTTPS、多进程、真实数据库验证。
- 微信原消息到“健康事实”的抽取仍是执行者的人工步骤；本轮没有做自动抽取、定时调度，也没有做报告图 OCR。
