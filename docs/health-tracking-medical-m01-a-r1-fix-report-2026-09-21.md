# HEALTH-M01-A-R1 收尾回执（脱敏）

**状态：HEALTH-M01-A-R1 收尾已提交审核。** 执行方自检；真实分析包仍为“待审核”，未采用，也不是医生审阅。

- 基线 main `dd9c917`。没有仓库程序改动，因此没有重跑全仓测试、构建或浏览器。没有连库、部署、调用外部模型或建调度；只处理卡内这一处，没有新一轮历史普查。
- 私有目录：`C:/Users/teddy/NianlifeOps/health-tracking/2026-09-21/health-m01-a-r1-fix/`（不进 Git）。旧账本、原导出、旧分析文件（`health-m01-a`、`health-m01-a-r1`）原样保留。

## 做了什么

- 四月病程（EP-C）分析用到的两条原聊天上下文（4 月 5 日 09:40:39 一方问“是否按时吃三天”、09:41:46“抗过敏药好像没怎么吃”）通过**既有** `import --adapter messages-json` 写入**新的私有工作副本**，成为账本来源实体：保留会话身份、发送时间、发言人、正文及正文 SHA-256（不是只有路径）。答复的三条本来就在账本里，已在引用中。
- 在新副本上追加分析版本 `AV-EP-C-2` 并提交待审核，两条来源作为 `source` 类引用进入依赖快照；`AV-EP-C-1` 追加“被取代”的退回记录，历史保留；其余 9 版正文与状态不变（在新副本上仍然有效）。
- 正文改为对“是否按时吃三天”的有限反馈：答复含糊，看不出是否按时或有无漏服，具体次数和完整疗程仍未知；没有删除已核实的上下文，没有改回错误解释。“快照缺失”的内部说明已从正文移除，技术说明只在本回执。

## 验证（只做卡内三项）

- 正常引用：AV-EP-C-2 可起草、提交，10 版状态全部有效。
- 上下文被更正后失效：分别更正两条来源之一（在一次性临时副本上），AV-EP-C-2 都变为“过期”，原因指向被更正的来源；其他版本不受影响。临时副本已删除。
- 旧输入不变：`health-m01-a/data` 文件哈希与制作前记录一致；r1 的分析文件与证据登记未改（仍为 10 版待审核）。

## 交付与最终采用

分析包 `health-m01-a-r1-fix/analysis/analysis-pack.md`，README 内有采用命令（`analysis adopt --ledger …/data/history-ledger --analyses …/analysis/analyses.json --evidence …/evidence/register.json --id AV-EP-C-2 --by <审核人> --basis "…" --apply`）。旧/新版本关系：r1 包保留；本目录是 r1 的 10 版加 AV-EP-C-2，采用应以本目录为准。

## 局限

- 两条来源来自本机原聊天导出的这两条消息，本轮没有重新核对导出文件本身；快照只感知账本内的更正，不监测导出文件变化。
- 其余局限同 R1 回执。
