# HEALTH-M01-A：病程总结与增量复核闭环执行卡

日期：2026-09-21。状态：Teddy 已同意下一步，执行卡已准备，待现有 Claude Code 会话 ACK；不代表已派发或开工。

## 目标与边界

HEALTH-01/02/03/04 已按约定范围关闭。沿用 HEALTH-04 r5 正式页面，不重新设计、不恢复已删除栏目。本轮让已核对的病程出现有内容的医学辅助总结，并走通“新资料 → 影响识别 → 新版分析 → 审核采用 → 页面更新”。只做文件账本本地实现，既不要求自动调用模型，也不能只交一个永久待核队列。

先读总计划、HEALTH-04关闭审核、最新页面模型/服务及健康导入入口。先回 ACK、当前 main 基线、预计完成时间和具体修改范围；不得回退当前 main。Codex 审核，Claude Code 实施，Teddy 手动转交，不启新会话或旧 A/B/C 调度。

## A. 补齐实际病程总结

从 HEALTH-04 已接入的接受基线和最新只读副本开始，沿用现有事实、规范病程和来源链，不重做历史全量阅读、不无限扩词。先最近一段，再覆盖其余已登记病程；同一病程在发热/呼吸道等分类下不重复分析、计数。没有实际病例的类别不编示例。

逐病程给出可读的短总结：发展特点、可以支持的医学解释、重要的不确定性、对观察护理/就医安排的实际影响。明确医生诊断、家长报告、汇总转述和辅助推断的层级；资料不足也要说明已经能判断什么及不能判断的原因，不能全部退回“问医生”。结束未知不能写已康复，历史状态不能冒充今天状态。

已有适用证据优先复用；不足处检索近期权威正文，记录条款、适用人群、版本/获取日期与链接，不为2025/2026年度差异耽误。公共检索不带孩子姓名、聊天原文或其他可识别信息。把事实引用和医学证据引用分开保存在私有分析包；页面只沿用“病程总结 / 观察与护理 / 就医安排”，不增加依据按钮或新栏目。

护理和就医建议给出适用前提与触发条件；建议不能自动成为预约或吃药提醒。不猜剂量、不擅自启停处方药，不把没有记录视作正常。没有新近家庭观察时，只写截至资料日期的解释及条件性措施，不伪称当前评估。医学分析是辅助材料，不能把 Codex 内容审查写成医生审阅。

## B. 新资料与新版分析接起来

复用 HEALTH-02 版本/依赖与 HEALTH-04 失效能力，不另建平行底账。

1. 新手记、更正、就医材料及微信增量通过既有入口进入；微信原消息是来源，健康事实提取和本人/角色/上下文确认是另一步，不能把每条消息直接变症状。未解读的报告图只记材料，OCR另批。
2. 产出可持久保存的差异与受影响病程清单，携带事实版本、来源绑定和未归属线索。明确未归属记录的去向，不按日期接近强行归属；疑似持续可作为待核候选，不能自动延长红色区间。
3. 新版分析采用追加版本：draft / 待审核 / 已采用 / 过期等状态应明确，并绑定依赖快照。生成或重新打指纹不能自行把旧文字变成已审核。采用时再次比较依赖；期间事实变了则拒绝采用旧快照，保留旧版供追溯。
4. 提供一个清楚的本地命令入口，默认预览差异；显式采用要记录执行人、时间和审核依据。页面读取已采用且依赖有效的版本，旧版失效仍提示待核。不要为了演示伪造 Codex 审核记录。
5. 本轮由你实际制作首批逐病程分析包提交 Codex 内容审核；程序更新闭环用合成内容验证采用。真实新包未通过审核前不替换已接受内容。无需外部模型 API、后台服务或每日定时任务，回执写清哪些动作自动完成、哪些需要执行者分析和 Codex 审核。

## C. 有限验证与交付

用少量有判别力的合成场景覆盖：新手记/微信增量进入后受影响病程正确；重放不增殖；更正使旧分析失效；审核期间又有变化时拒绝采用；通过采用后页面更新且无关病程不变；非本人/提问不成为孩子症状。可以合并测试，不按字段铺大量样例，不重复旧UI全流程。

真实基线只读，用新私有目录制作分析与差异，记录输入前后哈希。做一次手机浏览器核对，确认总结与两类措施实际出现，保留现有折叠和权限。必要的typecheck、lint、构建各一次；全仓测试一次，已知编辑器3项失败单列，不代修、不反复重跑。

交付：
- 私有目录：C:/Users/teddy/NianlifeOps/health-tracking/2026-09-21/health-m01-a/。真实个体分析、证据登记、影响清单和快照均在这里，不进Git。
- 仓库：通用实现、合成测试和脱敏回执 docs/health-tracking-medical-m01-a-report-2026-09-21.md。
- 回执逐项说明病程覆盖、证据正文阅读范围、尚缺资料、自动与人工步骤、检查结果及局限。给出 Codex 需要审核的分析包路径和操作命令，避免把家庭问题扩大成新问卷。

不连接生产/业务库/RDS，不安装PostgreSQL，不迁移、不部署，不调用额外外部模型服务，不开每日调度。只用当前main，保护其他会话改动及全部旧产物；精确提交并push。完成状态为“HEALTH-M01-A 已提交审核”，不要自行进入下一批或宣称医学审核通过。
