# HEALTH-02 独立审核与 R1 修订卡

日期：2026-09-21。审核代码 `3b144bb`，回执 `cffc770`。结论：**需修订 HEALTH-02-R1；尚未进入 HEALTH-03/04。** HEALTH-01 仍保持通过，不重开历史阅读。修订卡已准备，由 Teddy 转交原 Claude 会话；未收到 R1 ACK。

## 独立验证与保留成果

- 已读执行卡、实现、测试、SQL 草案和私有 dry-run 账本；确认远端 main 为 `cffc770`。
- 独立复跑 18 个目标测试全部通过，包括既有多进程测试。六个基线输入现有哈希与回执记录全部相符；未改写执行方产物。
- 保留独立健康领域模型、候选/背景区分、默认 dry-run、人工更正叠加、分析版本机制和既有故事路径隔离。没有发现本通路调用业务库。
- 本轮没有重复全仓 lint/typecheck/test/build；其结果仍是执行方报告，不提升为 Codex 独立复跑。没有运行 SQL，也没有完整重跑私有生成器。
- 独立脚本和实际结果：`C:\Users\teddy\NianlifeOps\health-tracking\2026-09-21\codex-review-health02\probes.mjs`、`results.json`、`lock-probe.mjs`、`lock-results.json`。均使用合成反例；私有账本只读核对计数和哈希。

缺少 Postgres 环境可按原执行卡如实保留为未验证，**不是本轮拒收的单独理由**。不要求连接现有库、安装服务或实施生产桥接来解除本轮问题。但文件账本也必须满足其已声称的并发与恢复能力。

## R1 必修范围（一次集中完成）

### A. 增量身份与真实格式

`ledger.ts` 同批次同身份异内容返回 `new + conflict`，`rejected=false`，随后实际只保存第一版。反序重放改变选择；第二个版本没有被保留。强弱身份对齐也依赖顺序：JSON→Markdown 一条，Markdown→JSON 两条。`slotIndex` 只收已有账本，不收本批新增。

修复：批内/批间、顺序互换均不得任意选第一条或重复计数。冲突可保留为可审核版本/待决输入，或整批明确拒绝零写入；不能报告成功却丢掉冲突正文。弱身份不能强行合并，需保留别名/证据及歧义。同内容跨格式补充强身份应可解释且不增殖。

`adapters.ts` 新 Markdown 正则不支持既有 WeFlow 的 `## 日期 时间 发送人` + 多行正文；合成的实际格式片段在已有 `parseMarkdownExport` 得到 1 条，新适配器得到 0 条。本轮应复用 HEALTH-01 通用解析/身份能力，对照实际导出结构做私有只读抽验，并用脱敏多行、转义、附件测试覆盖。非空但不支持/解析失败必须明确失败，不能空导入成功；会话/发送人/时间/身份缺失须有明确拒绝或待审策略。原始消息入库与健康事实提取的边界要写清，提供已核增量事实接入路径，不宣称纯 source 导入已生成健康事件。

### B. 更正身份、有效值校验与引用

`applyCorrection` 的字段更正幂等只比 after/field，不比目标：对 o1 使用 C1 后，把 C1 用到 o2，会返回 duplicate 而 o2 未改。另可更正 occurredAt 为非法日期，落账后 `checkInvariants=[]`。字段更正没有重验有效对象；链接 afterRole 也需验证其适用类型。

修复：同更正 ID 必须核对完整规范化请求（目标、类型、字段/角色、值及审计元数据），异请求拒绝；校验更正后的有效实体和引用不变量，非法日期/时间基准/单位/角色/路径不能绕过导入验证。更正 dry-run 真正执行相同只读验证，不仅打印 wouldApply。合法历史未知继续允许，不补猜。

### C. 依赖闭合、分析失效和差异

已复现：仅引用 e1 的分析，在 e1 成员事实被更正后仍 current；医院 canonical_fact 更正的 impact.episodes 为空；source 更新后缓存时间轴与从头生成结果不同；未挂靠事实的正文更正不出现在 diff。根因包括分析快照只有成员 ID 无成员内容、computeImpact 没遍历 source→observation / canonical→encounter→episode，以及块哈希缺来源依赖。

修复：明确依赖图并覆盖间接依赖；成员内容、医院事实、来源版本/有效引用和更正都应传递到受影响块及分析快照。保留未受影响块；缓存结果必须与全量结果一致。未挂靠事实也比较内容变化。来源引用须绑定所依据版本，不能回溯时自动替换成最新来源；removed/更正后的支持链须在 trace 与展示中生效。

### D. 完整适配和可审阅时间轴

现有时间轴只展示 observation：账本内 198 条医院 canonical_fact 没有进入时间轴正文，就诊只展示 ID；没有就诊号的 5 条规范事实被适配器直接跳过。私有未挂靠观察 501 条，Markdown/HTML 静默只渲染前 200 条。不能把这些输出称为完整历史时间轴。

现有来源映射还有损失：manifest 中同 document_id 有 5 组多原件，`byDocId.set` 只保留最后一个；丢弃原始 observations、primary_source、all_source_values 及原件位置，无法完整追溯多来源取值。医院历史更正 `run-20260920-r2/corrections.jsonl` 实际存在 10 条，含 original/corrected/basis/reviewed_at，不应以所选六文件没有前值为由全部排除。R4 病程有 9 行 handoff_refs，新适配器忽略，回执“只有话题级关联”不完整。

修复：医院事实/就诊/处方与微信事实共同呈现，保持预约、仅检查、已就诊和实际服药区别。缺就诊号的本人事实保留为未挂靠，不伪造就诊；非本人排除可保留。还原来源一对多、已有观察与规范事实关系、字段依据和历史更正链；不能编造审核者或新更正。按已接受终态接入 handoff_refs，保留转述层、不重复计数；canonical/group/版本关系及依据不得丢弃。所有内容均可审阅，若分页/节选明确标注并给完整入口；汇总稿“handoff”不得显示为药物交接。无需产品 UI 重设计。

### E. 活锁保护和真实中断恢复

`file-store.ts` 只按锁目录年龄删除锁，不核对持有者是否存活或锁身份；release 也无所有权校验。独立两进程反例缩短 staleLockMs：第一写入仍活着，第二写入抢锁完成；两者 exit 0，预期 revision 11，实际 1。默认 60 秒只是推迟同一问题。

修复：活着的持有者不能因超时被抢；回收和释放必须校验同一所有者/令牌，避免旧持有者删除新锁。补慢写入超过租期、多等待者、实际终止持锁子进程后的恢复、写前/提交附近中断与重试测试；区分进程崩溃和断电持久性，不夸大后者。所有测试仅隔离合成文件账本；无需现有数据库。

### F. 私有输出边界与失败报告

`assertLedgerOutsideRepo` 对仓库根路径 rel==空串放行；CLI 又公开 allow-repo-path 绕过，timeline --out、--report 未做同样约束。独立探针确认根目录被允许。修复所有真实资料输出的规范化路径检查（含根路径和目录链接解析），取消生产 CLI 的绕过或限制为明确合成测试路径。不要改写既有真实产物。缺字段、空解析、引用错误、冲突和中断的退出码与报告须一致、不得夹带正文到普通错误日志。

## 实施边界与验收

- 先回 ACK、实际最新 main、拟改路径和阶段 ETA；只修上述第二批缺陷，不扩词、不重读第一批全量资料、不新增医学结论。
- 允许路径沿用 HEALTH-02 执行卡，必要时扩展通用健康解析测试。提供面向后续 V2 接入的明确领域/存储接口与字段映射；本轮不要求落地生产桥接，不能宣称已完成接入后的故事保护验收。
- 本卡反例均纳入合成回归；新增反例测试正确行为，不硬编码探针字面值。更正/source/医院/未挂靠增量必须检验全内容差异和分析状态，不只行数。
- 新私有输出 `C:\Users\teddy\NianlifeOps\health-tracking\2026-09-21\health-02-r1\`；本轮审核目录、health-02-a 及第一批目录只读保留。重新核对本人规范事实保留、来源和更正引用、转述病程链接及完整时间轴，保留逐项差异。
- 运行目标测试与项目规定 lint/typecheck/全仓测试/build；无代码变化的既有证据可明确复用。真实 Postgres 仍可列未验证，不把文件锁结果外推。
- main 精确提交并 push，保护其他会话改动。不连生产、不写现有业务库、不迁移、不部署、不调用外部模型。
- 交付 `docs/health-tracking-batch-02-r1-report-2026-09-21.md`，逐项给证据和剩余限制，状态“已提交审核，等待 Codex”；不自行进入下一批。
