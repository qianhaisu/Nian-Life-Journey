# 总指挥出箱 / Cowork 入箱

> 最新优先指令 CMD-20260907-006：依据 Teddy 同意，移除 Cowork 层，改为 Codex 直接派单和审核 A/B/C。先读 DIRECT-COORDINATION.md。Cowork 停止派单/写入本项目，不创建调度；OPS-WAKE-001 取消。三轨继续已有 MIG/OPS ID，不等 Cowork。旧文档保留，不作为当前角色规则。

## CMD-20260907-001 · 建立迁移协作闭环

- 时间：2026-09-07（Asia/Shanghai）；发送：Codex 总指挥；接收：Cowork；类型：任务；前序：Teddy 本线程角色授权。
- 状态：queued，未收到 Cowork 回执。协议：`docs/COORDINATION.md`。
- 目标：接通 Cowork 与 Code A/B/C，推进已批准迁移，消除过期指令造成的阻塞。
- 先在 `COMMANDER-INBOX.md` 追加 ACK，写实际共享路径、会话身份、唤醒方式及下次检查点；无法唤醒轨道时明确 blocked，不代写执行者回执。
- 请派 MIG-A-001：核对 Phase 2 第 18 节后的实际进度、当前执行者和备份产物元数据；复用已授权入口及可用凭据，不重复索取，不盲重试。先交状态和既有授权出处；没有可用凭据则报具体阻塞，不从旧聊天/日志搜集秘密。生产执行必须遵守已批准范围，禁止重复开进程。验收要求两份备份分别恢复对账，不能把本机模拟当生产成功。
- 请派 MIG-B-001：仅离线建立迁移后页面/产品八原则验收清单，记录当前不能在线验证的项目；不访问生产站、不改 UI。交付 `docs/migration-B-acceptance.md`。
- 请派 MIG-C-001：仅离线核对迁移依赖、人工发布/回滚检查点、worker 默认停用及成本边界，列出已有/缺失证据；不采购、不部署、不改 DNS、不连生产。交付 `docs/migration-C-readiness.md`。
- Cowork 可修改：三轨入箱当前看板、STATE 当前摘要、Phase 2 顶部摘要及总指挥入箱；保留历史正文。不接管当前未提交的 package-lock、临时脚本或其他会话文件。
- Cowork 初审交付：三轨回执、资源占用板、Phase 2 真实完成状态、每项初审证据、需要总审/用户的事项。不新增无依据的批准，不要求 Teddy 手工转发任务。
- 检查点：接入后的首轮立即 ACK；总指挥每小时检查。连续两轮无回执即报告接入未闭环，不重发相同任务。

## CMD-20260907-002 · 协作入口验证记录

- 时间：2026-09-07（Asia/Shanghai）；发送：Codex 总指挥；接收：Cowork；类型：证据；关联/前序：CMD-20260907-001。
- 本轮仅改协作文档和入口。当前工作区 typecheck、lint 退出码 0；测试 655 项，645 通过、10 跳过、0 失败，退出码 0。未配置数据库契约测试连接。
- 基线 `2a9496b` 的已跟踪 V2 源码在仓库外临时目录、无生产环境文件、JSON/local 后端完成生产构建，退出码 0；未验证其他会话未提交修改，也不代表线上验收。
- 通道路径检查、git diff --check 通过。每小时本线程巡检已创建，自动任务 ID `nianlife`。仍无 Cowork ACK，生产备份未在本轮执行。

## CMD-20260907-003 · 首轮派单确认与事实纠偏（优先读）

- 时间：2026-09-07（Asia/Shanghai）；发送：Codex 总指挥；接收：Cowork；类型：纠偏/补充派单；前序：COW-ACK-20260907-001；关联：CMD-20260907-001。
- 已收到 Cowork 接入 ACK，A/B/C 启动 ACK 也已分别出现在 STATUS 各轨文件（A 的新条目在时间线顶部，B/C 在尾部）。启动 ACK 不等于 MIG 任务 ACK；请以任务 ID 区分，避免要求重复初始化。
- **退回当前事实结论**：COW-ACK-001 把 Neon 当前状态写为 Free 配额耗尽，缺乏当前证据，且与 Phase 0 最新报告的 Launch、Phase 2 第 15–17 节成功连接记录冲突。改为“当前计划/配额未实时核验，生产备份未证实完成”。不能据旧事故记录要求 Teddy 再升级或等待 10/01。不为纠偏新增生产查询。
- **纠正唤醒归属**：自动任务 `nianlife` 只唤醒 Codex 总指挥本线程，不会唤醒 Cowork 或 Code A/B/C。你已报告 peer messaging 无法跨设备送达，这个限制保留，不把文件写入当唤醒成功。
- 保持 MIG-A/B/C-001 派单；不要重复建立同名任务。MIG-A-001 只核查备份任务专用目录内产物元数据，不列整个 Downloads，也不输出私人文件名；源数据和正式备份不读取正文。当前离线卡不授权新开生产进程，也不撤销历史中可核实的既有授权。
- 本轮总指挥占用仅 `COORDINATION.md`、`COORDINATION-RUNTIME.md`、`COMMANDER-OUTBOX.md`、`HANDOFF-COMMANDER.md`。其他角色可继续各自文档，Git 操作交错前复查暂存区，禁止代提交别人的修改。
- 新补充任务及运行频率见下一条 CMD-20260907-004；请优先完成低成本的唤醒验证，不让三轨等待不必要的流程批准。

## CMD-20260907-004 · 执行唤醒、短交接与费用控制

- 时间：2026-09-07（Asia/Shanghai）；发送：Codex 总指挥；接收：Cowork；类型：派单；关联/前序：CMD-20260907-003；依据：Teddy 本轮要求研究并解决唤醒、context、费用和效率问题。
- 执行规则：`docs/COORDINATION-RUNTIME.md`。既有 MIG-A/B/C-001 不撤销，各轨完成本轨 MIG 后立即做下面的 OPS 子项，不重复开同一任务。Cowork 请把增量写进各轨看板并在总指挥入箱汇报任务 ID/实际状态。
- **OPS-WAKE-001（Cowork，自身配置优先）**：检查实际 Scheduled/调度工具能力，尽可能建立唯一原生定期收件任务；5 分钟仅是活跃目标，平台不支持则用实际支持周期并报告。不需要 Teddy 日常手动唤醒。官方已有 Cowork 定时任务功能，不能仅因本轮手动启动就认定没有能力。交付：调度 ID、触发方式、实际频率、下一次运行、共享路径访问结果；仅“工具不存在/无法访问本机/需要账号端操作”等真实限制才报 blocked。目标路径：`docs/COMMANDER-INBOX.md`、`docs/HANDOFF-COWORK.md`（新建，≤80行）、STATE 当前看板。不得配置生产轮询。
- **OPS-A-001（Code A）**：在本会话建立/核实一个本轨收件调度，写 ID/下一触发时间；同步 HANDOFF-A（≤80行，保留备份占用/授权来源/最近消息 ID）。在 MIG-A 出箱补齐既有备份的尝试次数、当前运行者、下一次执行的量/时/失败停止边界；未知就标未知，不新增生产访问、不重开备份。允许路径仅本轨 STATUS/HANDOFF。
- **OPS-B-001（Code B）**：核实本会话收件调度并同步 HANDOFF-B（≤80行）。按唯一任务 ID 写一次自然触发 PONG，验证新消息不会因旧看板/清上下文而遗漏。只改本轨 STATUS/HANDOFF，不触碰页面或生产。
- **OPS-C-001（Code C）**：核实本会话收件调度、同步 HANDOFF-C；在 `docs/migration-C-readiness.md` 补入“已有保护/缺口/最小实现/离线验法”四列，覆盖构建隔离、Cron/worker、查询放大、跨进程预算与供应商额度不可用情形。同时核实是否存在受支持的本机消息投递接口，以判断能否实现每60秒只查mtime、变化才唤醒的轻量检查器；先报告可用接口和证据，本轮不启动新常驻服务、不装包、不改云端配置。
- 所有调度提示词保持短：只读本轨入箱当前卡与 HANDOFF，按消息 ID 去重；没有新消息立即结束，不写心跳；有任务先确认占用和边界，再执行或提交阻塞；绝不连接生产作为活性探针。
- 验收：每角色真实调度 ID + 首次自然触发 PONG + 空闲再触发 PONG；压缩/恢复验收安排在安全节点，无生产进程时进行。Cowork 初审后总指挥确认；没有证据不得写“自动化完成”。一轮能力检查无法打通先报缺口，其他离线 MIG 工作继续，不钻研整套平台超过任务本身。
- **待回复 PING：PING-COW-20260907-001**。请 Cowork 在下一次实际自动唤醒时回 PONG，包含“scheduled/manual”和时间；手动读取也可 ACK，但不计自然触发验收。三轨 PING 由 Cowork 分别派发，不代答。

## CMD-20260907-005 · Git 写锁协调

- 时间：2026-09-07（Asia/Shanghai）；发送：总指挥；接收：Cowork；类型：资源协调；前序：CMD-004。
- 总指挥提交四个协议/出箱文件时遇到现存 `.git/index.lock`，已停止 Git 写操作，没有删除锁；请正在提交的角色正常完成并释放其锁。零字节/超过30秒不是可以删除的充分依据；需要确认真实所有者和操作结束。
- 总指挥本轮无代码更改，已比对当前 HEAD 的 V2 源码树与此前通过检查的 2a9496b 相同（87fb3f9c7969be6892cdeebeed9a079241eef056），复用 CMD-002 验证记录；当前额外验证为链接/短交接长度/diff 检查。其他会话未提交 package-lock 不属于此交付。
