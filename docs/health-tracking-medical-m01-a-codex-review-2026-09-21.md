# HEALTH-M01-A 独立审核与 R1 执行卡

审查 e13720d（origin/main已核对）。结论：**更新骨架与病程覆盖成立，医学包暂不采用；限定三组补正后复审。**

## 已成立与本次核查范围

- 独立运行 health-analysis 与 health-page：18/18通过。草稿/提交/采用生命周期、账本更正失效和既有页面回归已成立，不重做。
- 阅读新增版本、影响识别、页面接入实现和10个视图的分析文字，并定点对照事实包及一处原聊天上下文；不是重新核对全部原件。
- 浏览器11项、真实输入哈希、typecheck/lint/build沿用执行方证据，本次未独立重跑。全仓既有编辑器3项失败仍单列，不代修。
- 私有审核证据（含合成反例和具体医学/事实问题）：`C:/Users/teddy/NianlifeOps/health-tracking/2026-09-21/codex-review-health-m01-a/README.md`、`probes.mjs`、`results.json`。真实包未改、未采用。

## R1-A 医学文字和实际引用一致

先读私有README的7项定位，只修当前包，不全量重读历史。紧急表现不能合并写成“当天去看医生”；在现有就医安排内部明确立即行动与当天联系的差别，不新增UI栏目。参考独立已读的[NHS儿童发热正文](https://www.nhs.uk/symptoms/fever-in-children/)与[WHO肺炎正文](https://www.who.int/news-room/fact-sheets/detail/pneumonia)。NICE本站本次403、镜像验证码，未宣称独立阅读全文成功。

校准从单个检验/影像推导病因或严重程度的越界结论；正常/阴性结果不能成为不充分的安心依据。没有适用正文的解释收窄到事实与具体问题，仍保留有根据的护理和就医指导，不退回全是“问医生”。

纠正私有README列出的上下文、预测当阴性、不同日期当同次冲突、日期截止与跨病程引用混用。逐条确认本版实际使用的论据进入factRefs/依赖：包括其他病程、转述、未挂靠事实；区分病程末次记录和分析读取资料截止日期。报告单数字带单位，不再新增年龄精度猜测。近期适用资料即可，不追年度差异，不为未用到的医学问题扩大检索。

## R1-B 分析正文与医学证据必须随审核绑定

独立反例：不存在的证据ID仍可adopt；提交后替换summary且不更新bodyHash，也可adopt，页面照常显示且review为null。这不是账本依赖更正问题，而是被审核内容本身未被验证。

要求：采用及读取时校验分析结构、内容hash与审核绑定；不一致拒绝采用/明确不可作为有效分析，保留历史。医学证据ID须能解析到实际登记和已读依据版本，绑定登记与正文快照/摘要哈希；证据不存在、不可核验、被修订或撤回时不能继续有效。不要只对旧materials检查，新分析的措施也要覆盖。无需联网逐次抓指南，可用已审本地证据包，不额外调用模型。

补“提交后正文变化”“不存在的证据”“已采用后证据变化/缺失”“跨病程实际引用更正”的合成反例及正常对照，不做字面串拦截。

## R1-C 待完成的复核不能被新草稿和基线遮住

独立反例：v1已采用→依据更正→写v2草稿→当前账本存新基线。页面仍显示过期v1，但impact只选择最新草稿v2，affectedEpisodes变成空。分别检查正在展示的已采用版和待审核版；更新导入基线不能让尚未完成的复核消失。加一个连续场景即可，不扩为调度系统。

## 交付与授权

执行前回ACK、当前main基线和ETA。保留旧包，输出新目录 `health-m01-a-r1/`；仓库回执 `docs/health-tracking-medical-m01-a-r1-report-2026-09-21.md`。先解决以上三组，提交新版分析包供Codex审核，真实包不自行采用。测试只新增上述反例并复跑受影响目标；集中工程检查各一次，不重复旧浏览器全流程。

UI不变，不重开已关闭批次，不连接库/RDS，不安装PostgreSQL、不部署、不调用额外外部模型、不建定时任务。保护其他会话改动，精确提交并push当前main，不自行进入下一批。
