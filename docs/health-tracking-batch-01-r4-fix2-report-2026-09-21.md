# HEALTH-01-R4 二次补正回执（脱敏）

日期：2026-09-21。**状态：已提交审核。** 不是审核通过，也不进入 HEALTH-02。

| 项 | 值 |
|---|---|
| 基线 | `b3cd129` |
| ACK 提交 | `f2d5825` |
| 上一轮交付 | `8383ded`（及其私有目录，**原样保留**） |
| 私有输出 | `run-20260920-r4-fix2`（新建）；`run-20260920-r4`、`run-20260920-r4-fix` 与全部审核目录逐一核对未被改动 |
| 依据 | [R4-fix 复审](health-tracking-batch-01-r4-fix-codex-review-2026-09-21.md)——**只做其中的三项清单** |

本回执脱敏：不含健康原文、个体指标、药品信息、患者/会话/消息标识。健康内容留在仓库外，**不进 Git**
（这不等于「未离开本机」，外部服务披露见第五节）。

## 一、清单三项，逐项结果

### 1. 93 条日期窗口关联：逐条核定

我的错：日期窗口里只剩一个候选就直接算入病程，**把「时间接近」当成了归属证据**；
「其他话题」规则只在没有日期候选时才生效，所以没挡住。

现在：这 93 条**逐条复核**，结果 **69 条保留、24 条降为候选/同期背景**。
- 保留的都有原文或发热语义表明写的疾病/事件关联（体温读数、退热用药、呼吸道症状、当日就诊等），并记录依据；
- 降级的是只有时间接近的：其他部位问题、外伤性护理、事由未知的就医记载、**回忆性消息（无原文发生日，不按发送日归属）**、
  一般照护与托育周记，以及一条**提醒**（提醒不计治疗事实）。
- 候选事实**保留**，登记在各病程的 `candidate_fact_ids`，**不计入该段的症状/治疗/就诊事实**，也不据此主张新病程。
- 生成器不再自动归属：未登记、又没有明确关联依据的，直接报错，逼着逐条复核。
- 审核点名的五处坏例全部落在候选里。

### 2. 两条消息角色与支持链

两条事实的主引用其实是**计划/提醒**，摘要却承载邻居的观察，支持链为空。已拆开：主引用记为计划，
真正的观察绑到原消息并带支持链（其中一条还带上前置的照片消息）。
同一规则（观察类事实 + 主引用含计划/提醒标记 + 支持链为空）扫全表，无其他命中。

### 3. 反例与终态校准

- **反例 50 → 64**，全部断言检查器返回 False。新增：足趾事实移到另一段并同步引用数（审核的独立反例）、
  叮咬事实挂进呼吸道病程、回忆性消息按发送日挂靠、事由未知的就医记载被挂靠、复核表缺行、候选仍计入该段、
  计划/提醒承载观察（两种）、服药报告与执行状态不符（四种）、夜间查看被弱化、交替用药限定被压缩。
- **执行状态区分**：`actually_taken` 现在是「有某次服药报告（药名、次数、完整疗程未知）」或「无任何服药执行报告
  （处方、交接、提醒都不算）」。审核点名的那条家属服药报告已计入；汇总稿的执行转述也按转述层计入。
  文档不再写「其余各段一律 unknown」。
- **护理措辞按 NG143 §1.6/1.7 校准**：夜间要**有意识地去看**，不能只在碰巧醒着时才看，原文没规定次数所以不写死；
  **不要同时使用**、**换用**（一种用后仍不舒服才考虑）、**交替**（仅在不适持续或下次可用时间前再次出现时才考虑）分别表述，不给剂量。
- 受影响计数、四份文档、覆盖表与快照均已重新生成。

## 二、需要收窄的一句话

上一轮回执说「每项检查都独立重推」。**要收窄**：调用同一个生成器重推，证明的是**确定性与内容一致**，
**不是独立的医学判断**；新增检查里的规则（其他部位词、计划标记、关联依据）也是执行方写的，
证明的是「规则被执行、不会被悄悄改坏」，不证明规则本身在医学上无争议。

## 三、检查结果

| 命令 | 结果 |
|---|---|
| 私有 `f_validate.py` | 退出码 0（正例 **22/22**，反例 **64/64 生效**） |
| 私有 `f_rebuild.py` | 退出码 0（`idempotent`；21 份全内容一致，唯一差异是快照里输出根目录的绝对路径） |
| `node --test test/health-audit-wechat-dedupe.test.mjs` | 37/37 通过（复跑） |
| eslint / typecheck / 全仓测试 / build | **本轮仓库无代码改动**，健康相关代码自 `8383ded` 起无差异，沿用上轮结果（0 / 0 / 1,507 项 0 失败 / 0），未重跑 |

## 四、未解决项

沿用 [R4-fix 回执](health-tracking-batch-01-r4-fix-report-2026-09-21.md)第五节；另：24 条候选事实**是否属于该段，
本批资料回答不了**，已如实登记为候选，不强行归属也不删除。

## 五、外部服务与数据流向、边界

本轮没有向任何外部模型服务发送数据，未新增外部调用或公开检索。历史披露不变：R1 曾把候选消息与上下文提交给
已配置的外部模型 API 做主体预分类，准确表述是「**不进 Git**」，不是「未离开本机」。
未连生产、未写业务库、未迁移、未部署、未回退 SHA、未新建分支、未强推、未改写历史、未修改其他会话的文件与既有目录。

**这是执行方自检，不是 Codex 审核通过。医学内容没有任何医生看过，不是诊断、不是医嘱。**
