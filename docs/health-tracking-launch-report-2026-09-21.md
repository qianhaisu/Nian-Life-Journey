# 健康模块上线报告（2026-09-21）

**状态：已部署到 https://nianlife.cn 并通过上线验收。** 这是执行方自检，交 Codex 复核发布结果。医学分析是辅助材料，经 Codex 内容审核，不是医生审阅。

## 部署事实

- **实际部署 SHA：`d1b23162261efe3b0888657c0d79d9ca3b2f2b29`**（线上 `/api/health` 返回该 SHA，容器 `nianlife-diag-web` healthy，镜像 `nianlife-web:d1b2316`，`--restart unless-stopped`）。这不同于本回执之后 push 的提交（`ecs-swap.sh` 参数分隔符修正与本报告，都不在镜像里）。
- 访问地址：https://nianlife.cn（首页四项菜单；健康时间轴 `/health`；爸妈记录 `/health/record`）。
- 发布前线上是另一会话在 22:41 发布的 `e4f9a48`（它已含健康模块代码，但没有配置，健康功能处于关闭状态）。本次发布相对它的差异只有：上线接入修正（原件目录映射与 Windows 分隔符、健康数据挂载与单写入检查、备份/恢复核验命令、Caddy 健康写入口、演练脚本、`.env.example`）及仓库文档；没有夹带其他会话未验收的应用改动。我等它发布完成、锁释放后才开始自己的构建。
- 首次 swap 尝试：我把两个挂载用空格传给 swap 脚本，被 ssh 拆开，新容器只挂了只读目录，脚本的“单写入者”检查发现可写记录目录没有挂载，**自动回滚到 `e4f9a48`（约几十秒抖动，无数据写入）**；修成逗号分隔后重试成功。这一步没有触发保留清理。

## 一、增量演练（合成数据，隔离临时目录）

`v2/scripts/health-rehearsal.mjs`，结果 `health-launch/rehearsal-results.json`，**19/19 通过**：
1. 真实 HTTP 处理器上登录并新增一条手记，同一请求重放不产生第二条；微信增量首次新增、重放全部识别为重复；新手记与本人未归属微信记录可见，别人的、提问的不成为症状；未归属线索被列出并给出邻近病程（不自动归属），重放后影响清单摘要不变。
2. 更正一个已引用事实 → 关联分析与措施待核，无关病程不变；旧版本在依赖已变后不能再次采用；待审核期间存新基线也不遮住旧版待核。
3. 合成新版分析 提交 → 采用（记录明确标“演练（合成，非真实审核）”）→ 页面更新，无关病程逐字不变；采用后影响清单清空。全部在系统临时目录并已删除，没有触碰真实账本或分析。

## 二、真实数据接入

服务器目录 `/srv/nianlife-health`（容器外，整体只读挂载，仅 `record/` 单独可写）：`ledger`（已接受历史账本，哈希与验收一致）、`pagedata`（区间/措施/派生记录）、`analyses`（health-m01-a-accepted，10 个已采用视图）、`evidence`（证据登记与已读正文）、`material-root`、`originals`（51 份医院原件，53 MB，逐份保留账本记录的 SHA-256；账本记录的原始根目录通过 `HEALTH_HISTORY_ORIGINAL_ROOT_MAP` 映射到服务器目录，账本不改写，所以分析快照不失效）、`record`（爸妈手记账本，上线时为空）。
接入前用 CLI 在同一份数据上校验：10 版分析 adopted 且有效，页面 129 个节点、无待核区间、后续措施状态 current；上线后隔离容器与线上都读到同样内容。

**发现并修掉一个真实上线问题**：账本里的原件相对路径带 Windows 分隔符，Linux 上找不到文件；已改为规范化后仍做包含与哈希核验，并加了回归测试（`d1b2316` 已含）。
**线上真实账本没有任何演练记录**：`record/` 在验收后仍是 0 个文件。

## 三、备份与恢复

- 首次上线前备份：`/srv/nianlife-health-backups/health-20260921-225046.tar.gz`（75 个文件，附逐文件 SHA-256 清单与压缩包校验），并已拉取一份到本机私有目录 `health-launch/backup/`。备份目录只增不删，权限 0700。
- **隔离恢复核验通过**：解到临时目录，压缩包校验 OK、清单 75/75 OK、12 个 JSON 全部可解析，临时目录已删，线上数据未动。备份目录里另有两个 `225014` / `225027` 的不完整残留（第一次脚本权限问题所致，只有 tar，没有校验文件），保留不删，请勿用作恢复来源。
- 数据在容器外，换容器、回滚不会覆盖；`health-data-install` 只替换只读部分，永远不碰 `record/`。**没有定时备份**（本次不建调度）：手记开始写入后，需要在每次发布前和必要时手动执行 `health-backup`。

## 四、访问与安全（线上，HTTPS）

- 未登录：首页不含任何健康详情；`/health` 只给登录入口；提醒接口、原件接口均 401；`POST /api/health-record/entries` 未登录 401（不是 405，说明到达应用并被鉴权拒绝）；其余方法/路径（其他健康路径的 POST、`PUT`/`DELETE entries`、其他 API 的 POST）仍是 Caddy 405；跨来源写 403；错误密码 401。
- Caddy 只新增精确的健康写入口：`session`（POST/DELETE）、`entries`（POST）、`entries/<id>/corrections|attribution`（POST），请求体上限 45MB；其余照旧只读。已 `caddy validate` 后热加载，旧配置留 `.prev-<时间>`。
- 妈妈登录（线上真实密码）：会话 cookie 为 Secure + HttpOnly，`/health` 响应 `private, no-store`；33/33 个页面里的医院原件链接登录后均 200 且逐份通过哈希核验。
- HTTPS：Let's Encrypt 证书正常（到 2026-12-14），HTTP→HTTPS 308、www→主域名 308。
- 凭据：会话密钥和爸爸/妈妈密码在本机私有文件 `health-launch/secrets/health-secrets.env`（不进 Git、不出现在本报告和日志里），服务器上只在 `0600` 的运行时 env 源文件里；**需要 Teddy 从该私有文件取出密码转交爸爸、妈妈**。

## 五、单写入实例

- 现有 swap 是“先停旧容器、再起新容器”；新脚本在起新容器前后都检查“运行中且挂着可写 `record` 的容器必须恰好一个”，不满足就自动回滚（上面首次尝试已实际触发过一次）。回滚容器是停止状态，且旧版本没有该挂载。
- 文件账本的锁只在同一台机器、进程可判活时才可靠，跨容器不能当作已验证的并发能力，所以**不允许两个健康写入实例同时存在**；本次也没有验证多实例并发。

## 六、精简验收（线上）

- curl 验收（`live-acceptance-curl.txt`）：上面第四节全部通过。
- 手机浏览器 390×844（`live-acceptance-browser.json`，截图在 `health-launch/screens/`）：底部四项菜单“首页/记忆/健康/妈妈月报”；首页未登录不含健康详情；妈妈登录；健康时间轴（默认 2026、可切换 2025、有“记一笔”入口）；10 个病程分析出现，病程与措施默认折叠，无横向溢出；最近病程展开后有总结、来源层级（含托班记录）、“已采用”与“不是医生的诊断或意见”；观察与护理 8 项、就医安排 13 项，“立即行动”与“当天联系医生”分列；原件读取 200 image/jpeg；无页面错误。其中一条自己设的断言阈值（节点数>20）设错，实际渲染是滚动窗口内的节点，与页面模型（2026 年 77 个）不矛盾，已在结果文件里标注。首页登录后没有健康提醒是正常的（账本里没有将来的预约）。
- 写入流程在**隔离合成容器**里用同一镜像验证（`syn-verify-result.txt`）：登录、cookie 属性、新增手记、重放同 id、跨来源 403、页面出现新手记、匿名不泄露、33/33 原件 200、隔离目录写入而真实 `record/` 为 0 文件；容器与临时目录已删除。线上没有做任何写入。

## 七、回滚点与 ECS 保留

- 当前：`nianlife-diag-web`（`d1b2316`，healthy）。回滚版本：`nianlife-diag-web-pre-d1b2316-20260921-230312`（镜像 `e4f9a48`）与 `nianlife-diag-web-pre-e4f9a48-20260921-224146`（镜像 `946b1e3`），均为停止状态。回滚命令 `deploy-ecs-public.sh rollback-app <容器名>`（会先停当前容器再启旧的）；回滚不影响 `/srv/nianlife-health`。Caddy 可用 `Caddyfile.prev-20260921-230345` 还原后 reload。
- 成功健康检查后自动保留清理已执行：删除更早的 `946b1e3` 之前的回滚容器 1 个及无引用镜像标签；磁盘 39943 MB 总量、9403 MB 已用、28695 MB 可用。

## 八、检查

- 代码有修改，按要求：健康目标测试 30/30（含新增原件映射与 Windows 分隔符用例）、typecheck、lint、本地生产构建各一次通过（构建改动的 `next-env.d.ts`/`tsconfig.json` 已还原）；ECS 上 `docker build` 成功。未重跑全仓测试；编辑器排期 3 项既有失败（另一会话 `e509e17`）不在本次范围，未代修。

## 未完成与注意

- 没有定时备份和监控；手记开始写入后请在发布前手动备份，恢复演练已通过但只做过一次。
- 单写入实例是运维约束，不是应用内的强制保证；应用内文件锁不跨容器。
- 需要 Teddy 把两个密码转交爸爸、妈妈；忘记密码或更换需要重新走 `health-env`（新建 env 源文件）并重新 swap。
- 医院原件在服务器上是复制件（53 MB）；账本没有记录之外的新原件不会自动同步，需要再次 `health-data-install`。
- 本次没有做 OCR、预约、每日自动接入或数据库迁移；没有安装本机 PostgreSQL，没有接 RDS 业务库读写健康数据。
- 没有真实手机/读屏/深色模式验证。
