# HEALTH-01-R4 交付回执（脱敏）

日期：2026-09-20。**状态：已提交审核。** 不是审核通过，也不进入 HEALTH-02。

| 项 | 值 |
|---|---|
| 基线 | `c87ea7b`（ACK 时的 main HEAD，未退回 `376e0a2`） |
| ACK 提交 | `a356fc6` |
| 执行区间 | 2026-09-20 23:04 起（ETA 90–120 分钟，实际落在区间内） |
| 私有输出 | `run-20260920-r4`（新建）；前四个运行目录与三个审核目录**原样保留** |
| 本文件依据 | [R4 执行卡](health-tracking-batch-01-r4-execution-card-2026-09-20.md)、[R3 审核卡](health-tracking-batch-01-r3-codex-review-2026-09-20.md) |

本回执**脱敏**：不含健康原文、原图、患者标识、家庭消息、会话标识。
所有健康内容留在仓库外的私有目录，**不进 Git**——这与「未离开本机」不是一回事，
外部服务披露见下。

---

## 一、五项关闭门槛，逐项证据

### ① 已知阅读范围全部有可复核处置；未读不转给家庭

597 条（R3 已登记的召回缺口）**逐条**有 `(处置码, 具名规则, 引用, 说明)`，
编号 1..597 恰好各一次（检查 V2）。处置分布：

| 处置 | 条数 | 含义 |
|---|---:|---|
| `inc` | 73 | 新增健康事实，已入事实层 |
| `sup` | 73 | 支持某条已入账事实——**必须有 link** |
| `dup` | 17 | 与已入账事实的重复表述——**必须有 link** |
| `ctx` | 103 | 相关但非已发生事实（提问/计划/建议/转述/替代疗法/科普） |
| `xo` | 69 | 他人主体（成人、他人小孩、猫） |
| `xn` | 260 | 无健康内容，按 6 条具名规则逐条落行 |
| `unc` | 2 | 读后仍不确定，已进疑点队列 |

共 **31 条具名规则**。R3 被判不合格的原因是 546 条共用一个兜底规则、没有个体归属；
本轮**没有「其余一律……」这种写法**，每条都单独落行。

「清楚无关按规则批量处置」是审核卡允许的，但规则要具名、要可复算——
比如 `R-DIAPER-LOGISTICS`（尿不湿领用/携带/更换流水，无性状或皮肤描述）。

**疑点队列 19 条**，按「谁来回答」分流：执行方已解决 3、材料缺失 7、家庭当前观察 5、医生判断 4。

### ② 旧事实未因迁移丢类型或来源；已知发生时间与消息时间分离

- 迁移改用**显式字段适配器**并对每个必填字段断言：取错字段直接抛错，
  而不是安静产出 `unspecified`。终态 `unspecified` **0 条**、会话为空 **0 条**
  （R3 分别是 85 和 87）。`episode_id` / `encounter_id` / `subject_basis` / 消息引用全部恢复。
- 两条已成立的改判（送药到托班＝**交接**、提醒摇匀＝**提醒**）在迁移表里单列，不会被改回。
- 发生时间按三条规则登记：原文显式回溯（14 条）、病历相对时间按**区间**（1 条）、
  原文即时读数（29 条）。合计 **44/650**；其余 **606 条保持 null——原文没给，不猜**。
- 复合事实拆分：R3 让**一条问句**同时承载体温、未用药、症状顺序三件事，
  实际三者来自另一位说话人相邻三条消息。已拆成三条并互相补 `supporting_message_ids`。

### ③ 当前病程覆盖有效历史记录；无伪造发热；无无故退出计数

- **当前病程 10 段**：R2 的九段**全部保留并计数**（R3 曾让其中八段无替代地退出，
  烫伤、医院确诊与最近一段就此从当前视图消失），加 R4 新建的 2025-11 段。
  **退出计数的 0 段**（V10）。
- **15 次就诊全部归属当前病程**（V11）。同一次就诊被两段共享的，计数时只算一次。
- 结束日未知一律 `null`，**不压成起始当天**，也不按「几天没消息」自动断病。
- 发热改为**逐条语义判定**，只做辅助视图、**不生成病程**：阳性 29（其中有读数 19）、
  阴性 5、预测 3、提问 6、监测意图 1、归因 1、家长概括 3、他人说法 1、接种顾虑 2、他人主体 1。
  阳性发热日 **7 天**。
- 无法判断是否同一病程的，显式标 `undecided`，**不强行合并或拆分**。

### ④ 正例通过、八类负例全部失败、快照完整、全内容重建幂等

**14 项检查全部改成语义守恒**，不再是「字段非空/格式合法」。关键几条：

- 医院层守恒：269 个观察被 203 条规范事实**各引用恰好一次**——删一条会留孤儿，
  复制一条会被引用两次，两种都当场暴露。
- 真实来源定位：**650 条事实全部回原件重读**，比对正文哈希、发送人、时间三项。
- 更正值：按**纸面原件逐字段比期望值**（7 条处方的规格/剂量/频次），
  而不是「只要不等于旧值就算过」。

**正例 14/14；反例 26/26 全部生效**，每条都真断言检查器返回 `False`。
其中前八条**逐字对应独立审核点名的八类破坏**：

| 反例 | 破坏 | 被哪项检查挡住 |
|---|---|---|
| N01 | 删除末条阅读登记 | V1 输入身份守恒 |
| N02 | 删除一条医院规范事实 | V5 观察↔规范事实守恒 |
| N03 | 复制一条医院规范事实 | V5 |
| N04 | 删除一条已纳入的微信事实 | V3 处置↔事实双向映射 |
| N05 | 格式合法但不存在的消息身份 | V6 回原件定位 |
| N06 | 非空但错误的正文 | V6 |
| N07 | 保留更正标记但篡改字段值（**任意**错值） | V8 期望值比对 |
| N08 | 无依据的发生时间 | V9 |

快照 **23 份文件**，含四份医学文件、医学证据表与覆盖表，哈希与盘上一致。
**全内容幂等重建**：在独立目录重跑整条链，逐字节比对 15 份产物全等；
唯一差异是快照里记录输出根目录的绝对路径字段，随重建位置变化，属非业务字段，已单列说明。

### ⑤ 四份医学文件与覆盖表、队列共用同一终态

四份文件**整份重写**（不打补丁），全部由同一份冻结事实生成。
生成后由 V13 **逐行**检查 10 条已撤回表述没有作为当前论断复活
（历史引用行豁免，因为「已撤回 X」本身不是在主张 X）。

家庭问题**只剩一份清单**（R3 是前六问 + 末尾另追加五问并存）。

---

## 二、本轮改正的、我自己之前的错

1. **「2026-07 起点是 07-12」是错的。** R2 已依家长自述记「7月10日左右」，
   托育机构 07-09/07-10 的观察与之一致。07-12 只是段内**第一个体温读数**。已恢复。
2. **「最近一次有来源的体重是 2026-05-26」是错的。** 医院 2026-06-15 的病历里就有身高体重，
   比它晚三周。这条问题**不该问家里**，已从家庭清单删除。
   而且两次测量**方向相反**（身高 +2 cm、体重 −0.81 kg），作为未消解冲突并列保留，不取舍、不取平均。
3. **两段病程是我凭关键词造出来的**：一段的原文是家长预判「**可能**接下来再次发烧」，
   另一段的原文是「量过体温也都**正常**」。都已删除。
4. **R3 的验证器挡的是探针的字面串。** 反例确实断言了 `pass==False`，
   但被检查的语义是空的——这一点独立审核说得对。

## 三、本轮新发现

- 某次低热的体温读数，**原文只有三个字符的数字**，不含任何关键词，
  两轮候选扫描都漏掉了它；它是从上下文里读出来的，不是扫出来的。
  **这是「关键词召回是开放的」最具体的一个例证。**
- 同段另有两条此前未入账：一条是**阳性状态描述**（听起来像症状，其实是好转迹象），
  一条是家长的**频率概括**（不是逐次记录，不能计入发作次数）。
- 597 条里另有 **22 条实质健康事实**是 R3 归进兜底规则的，包括一段排便困难序列、
  早期哭闹时长的量化记录、一处皮肤所见、一段发热期内的进食拒绝、连续两日的排便阴性记录。

## 四、检查结果

| 命令 | 退出码 |
|---|---|
| `npx eslint scripts/health-audit/ test/health-audit-wechat-dedupe.test.mjs` | 0 |
| `npm run typecheck` | 0 |
| `node --test test/health-audit-wechat-dedupe.test.mjs` | 0（**32/32**，R3 为 27） |
| `npm test`（全仓） | 0（**1,502 项，1,491 通过，0 失败，11 跳过**） |
| `npm run build` | 0 |
| 私有 `r4_validate.py` | **0**（正例 14/14，反例 26/26 生效） |
| 私有 `r4_rebuild.py` | **0**（`idempotent`） |

## 五、仓库改了什么

本轮只动三个文件：

1. `v2/scripts/health-audit/wechat-export-dedupe.mjs`（+70）——新增两个**通用**函数：
   `buildMessageIndex`（按会话隔离的消息身份索引）与 `verifyCitations`
   （核验引用是否指向导出里**真实存在**的消息，且引用者记下的正文没被换过）。
   这正是挡住上表 N05/N06 两类破坏的能力，且与健康场景无关，任何引用导出的管线都用得上。
2. `v2/test/health-audit-wechat-dedupe.test.mjs`（+64）——5 个合成用例，27 → **32**。
   包括「跨会话同 ID 不得误命中」「凭空捏造的消息 ID 必须被抓出」
   「非空但错误的正文必须被抓出，且与『找不到』分开报」。**全部合成数据，无真实会话标识。**
3. 本回执。

## 六、未解决项（如实登记，不是返工清单）

1. **召回仍是开放的。** 本轮按审核要求**不再扩关键词**，覆盖只相对当前词表成立；
   媒体路径 1,043 条按规则排除、未逐条读。
2. 发生时间仍有 **606/650** 条未知——原文没给，不猜。
3. 医院图仍是**单人核对**，未经第二人复核、**未经任何医生复核**；部分无日期病历的日期为推定。
4. 汇总稿的上游资料**本批仍全部未提供**。
5. 医学证据中仍有 3 条只到检索摘要级；**它们不承载任何确定结论**，
   因此本批**不判断**发作次数是否达到「反复呼吸道感染」门槛——这件事交给医生。
6. **2026-09-19 之后没有任何记录。** 任何「现在是否安全」的判断都不能由本批资料回答。
7. 两次机构测量请求在语料里**没有结果**——不是没读到，是没有。

## 七、外部服务与数据流向

- **R4 本轮没有向任何外部模型服务发送数据。** 597 条的逐条处置、上下文补拉与四份文件的撰写
  全部由本会话直接完成，未经任何预分类；本轮也未新增外部服务调用、未做新的公开检索。
- 历史披露不变：R1 曾把候选消息与上下文提交给已配置的外部模型 API 做主体预分类，
  医院图 OCR 亦在 R1 完成。R2/R3/R4 均未重新调用。脚本未记单价，**无法给出金额**，
  只能提供请求计数。准确表述是「**不进 Git**」，不是「未离开本机」。

## 八、边界

未连生产、未写业务库、未跑迁移、未部署、未启 worker、未改页面、未回退 SHA、
未新建分支、未强推、未改写历史、未修改其他会话的文件与既有运行/审核目录。原件只读。

**这是执行方自检，不是 Codex 审核通过。医学内容没有任何医生看过，不是诊断、不是医嘱。**
