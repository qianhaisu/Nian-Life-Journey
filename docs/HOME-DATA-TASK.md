# HOME-20260913-DATA · 数据 Claude Code 启动任务

> **最新任务：HOME-RELAYOUT-DATA。先读 `docs/HOME-RELAYOUT-2026-09-13.md` 全文并执行数据轨部分。** 本轮以版式修复为主，冻结 1.5.0、提供真实素材与呈现核对；0016 已正式应用，不重跑迁移，不重开评分/轮换架构工作。下面保留首轮任务作为历史背景，冲突以新任务为准。

状态：已制定，等待实际执行者 ACK。发送者：Codex 总指挥；接收者：Teddy 刚 /clear 的数据 Claude Code。

## 先恢复这轮上下文

Teddy 已确认新版首页，并要求你与页面 Claude Code 开始实施。你负责首页数据契约、照片质量与轮换、待办自动保鲜；页面 Code 负责 UI。**现在开始实施，不只返回计划。** 不恢复旧 MIG/OPS、不创建新 agent 或调度器、不重跑上轮归档。

按顺序读：
1. `AGENTS.md`、`CLAUDE.md` 的当前开发/数据读取要求；其中过时的迁移与发布状态不能覆盖本卡。
2. `docs/HOME-IMPLEMENTATION-2026-09-13.md` 全文，它是本轮共同规格。
3. `docs/nianlife-product-principles.md`；`docs/STATUS.md` 只读最后的 `055e919` 收口与遗留项，不从头读历史。
4. 对方 `docs/HOME-PAGE-STATUS.md`（尚不存在表示未 ACK）。

在 `docs/HOME-DATA-STATUS.md` 创建真实 ACK：任务 ID、当前时间、当前 HEAD、你接手的文件、第一步。不要替页面轨写 ACK。

## 第一小步：冻结能用的接口

从 `v2/app/page.tsx`、`v2/lib/home-view.ts`、`home-recent-pick.ts`、`upcoming.ts` 和 `v2/lib/db/upcoming-store.ts` 读现有调用链。先交一个可编译、复用现有读取的首页接口/类型（建议 `v2/lib/home-feed.ts`），注明每个导出、状态和空值语义。不要在首页增加全库读取。

首个节点通过检查并 push 后，在自己的 STATUS 写 `CONTRACT_READY`、接口位置/签名、数据块说明和 SHA，页面轨即可接线。继续实现规则，无须等待另一次产品确认。

## 文件所有权

你可改：
- `v2/lib/home-feed.ts` 及新增首页选择/质量/保鲜模块；现有 `home-view.ts`、`home-recent-pick.ts` 的必要兼容修改。
- `v2/lib/upcoming*.ts`、`v2/lib/db/upcoming-store.ts`，以及确有必要的窄仓储方法。
- 首页质量评估所需的专用脚本和单元/集成测试。
- 确有需要时新增最小、非破坏性的持久化/迁移；先在 STATUS 写用途、影响与回滚办法，本轮先本地实现验证，生产应用随统一交付。
- `docs/HOME-DATA-STATUS.md`。

不改：`v2/app/**`、`v2/components/**`、全局 CSS、字体与页面稿、对方状态、旧箱与其他会话文件。既有全站媒体判据作为约束复用，不借首页任务重构它。

## 必须交付

1. 数据接口实际返回真实日期/当前年龄、一期主故事及合法配图、至多一个不重复近况、保鲜后的提醒。
2. 按共同规格实现照片资格过滤、重复分组、真实质量评分/明确降级、稳定期次和冷却、失效撤销。不是只加随机数。模型不进入 SSR；旧数据不重导，人工批准不伪造。
   先确认实际可用的视觉能力，不能用纯文字模型猜照片内容。若视觉服务不可用，完成可运行的确定性降级并明确标注 AI 评估尚未完成，不伪造评分或把它藏进“已实现”。
3. 按共同规格实现临时事项自动退场、重要事项待核实、证据完成/取消/改期、同源幂等。把陈旧采购从首页移出，不把它写成已完成，不丢来源。
4. 对已有范围做一次有界真实核验，输出候选与退场原因；照片/家庭原文/完整数据快照仅留在 `C:\Users\teddy\NianlifeOps\home-2026-09-13\data\`，不进 Git。
5. 关键测试覆盖：六小时内刷新稳定/跨期可换、图和故事不串、撤销覆盖缓存、候选不足和模型失败；历史消息今天导入仍过期、来源重放不续期、关键健康事项不因过期消失、未知不等于完成、时区边界。

开始只做本轮；已有媒体可见性、审核结论、原始档案与后台全局 Organizer 开关保持其现状。外部模型费用沿用已有授权，不再问预算；候选批次按共同规格有界执行。

## 与页面轨交接

- 读对方 STATUS，在你的 STATUS 回应接口问题；接口问题由双方通过文件解决，不让 Teddy 转述字段。
- 数据先拿首个 Git/构建时段；页面 Code 可并行准备字体和独立组件。所有 Git、全量构建和完整呈现链测量按共同规格串行。
- 页面说「可以测了 + SHA」后，才对其完整代码做呈现复核。不得在其修改中途测出一个数量并宣布数据回归。
- 完成后写 `READY_FOR_REVIEW`：修改文件、接口版本、SHA/push、四项检查、真实候选/事项证据路径、八原则结果、剩余未验证项。
- 不部署。本轮由页面 Code 统一集成和私有部署。
