# HOME-20260913-PAGE · 页面 Claude Code 启动任务

> **最新任务：HOME-RELAYOUT-PAGE。先读 `docs/HOME-RELAYOUT-2026-09-13.md` 全文并执行页面轨部分。** 用户已否定当前版式，旧稿不再是唯一视觉标准；暂停旧版发布，右侧大照片、左侧两行题签、左下角 ≤300px 轻便签。下面保留首轮任务作为历史背景，冲突以新任务为准，不重复字体/数据基础建设。

状态：已制定，等待实际执行者 ACK。发送者：Codex 总指挥；接收者：Teddy 刚 /clear 的页面 Claude Code。

## 先恢复这轮上下文

Teddy 已确认新版首页，要求直接实施，并追加：**删除首页最底部的月份与「全部记忆」链接，它们与顶部「记忆」重复。不要重新出设计稿。** 你负责页面、真实接口接入、联调和最终私有交付；数据 Claude Code 同时负责领域规则。

按顺序读：
1. `AGENTS.md`、`CLAUDE.md` 的当前开发要求，旧 MIG/OPS 不属于本轮。
2. `docs/HOME-IMPLEMENTATION-2026-09-13.md` 全文及其指向的唯一已确认 HTML。
3. `docs/nianlife-product-principles.md`、`docs/design/visual-system-v2.md`；以本轮用户已确认稿及追加删除为最新视觉要求。
4. `docs/STATUS.md` 末尾的 `055e919` 收口，以及 `docs/HOME-DATA-STATUS.md`（尚不存在表示未 ACK）。

在 `docs/HOME-PAGE-STATUS.md` 创建真实 ACK：任务 ID、时间、当前 HEAD、文件范围、第一步。随后直接实施，不再问是否按稿做，也不创建新的 agent 或调度器。

## 立即开始的工作

1. 实际打开当前私有首页与已确认稿，理解现有品牌、三个导航、媒体/故事/提醒交互。稿内的底部导航是用户明确删除项。
2. 数据轨冻结接口期间，先完成中文圆体自托管与首页局部样式、独立展示组件。数据轨写 `CONTRACT_READY` 后接其真实导出，再切换 `v2/app/page.tsx`。不要先导入不存在的模块把共享工作区弄成不能编译。
3. 一张主照片 + 对应真实故事 + 至多一条近况 + 紧凑提醒。顶部保留现有真实路由 首页/记忆/张年；移除原首页多段列表、独立照片组、回忆时间线与底部重复导航。其他页面的资料与入口保留。

## 字体和参考资产

- 定稿文件与哈希在共同规格。只读参考；不要将它的家庭正文、照片 data URL、硬编码日期/年龄/事件或静态预览切屏代码提交进仓库。
- 原始圆体：`C:\Users\teddy\.codex\visualizations\2026\09\13\01a099d7-35aa-7531-83c8-39abce4014d9\chill-round.ttf`。
- 字体许可：同目录 `chill-round-license.txt`；来源 `https://github.com/Warren2060/ChillRound`。
- 稿中的 `nian-round-preview.woff2` 只覆盖稿中文字，**不能直接当完整生产字体**。生产字体需覆盖未来中文内容（可做合理分片与 unicode-range），本地静态资源交付、保留许可，修改/子集遵守保留字体名规则。英文继续兼容站点 Nunito。
- 验证实际加载出的中文字形；不是改 CSS 名称后靠系统字体回退。不使用 Google Fonts 在线请求；不把整站其他页面顺手重设计。
- 色彩沿用定稿中的可读鼠尾草绿、蓝、陶红、深栗灰、燕麦白。强调语词由真实内容驱动；不能把所有故事都硬编码成 cold/hot 的彩色模板。

## 文件所有权

你可改：`v2/app/page.tsx`；首页作用域 CSS；首页相关展示组件；必要的现有 Header/Gallery 接线；本地字体/许可文件；对应页面测试；`docs/HOME-PAGE-STATUS.md`。

`v2/app/layout.tsx` 仅在本地字体加载确有需要时最小修改，不顺带改整站信息架构。共享组件改动必须回归 /memory、一个月页、/about 和故事详情。

不改：数据轨的首页接口与选片/保鲜逻辑、`v2/lib/upcoming*.ts`、仓储/数据库/Organizer/导入脚本、对方状态、旧箱与其他会话文件。

## 页面必须是真功能

- 日期和年龄使用产品时钟；每段过去内容带自己的日期与当时年龄。设计稿日期只是预览，不写死。
- 照片从真实接口/现有媒体路由加载，用现有查看器打开原比例内容；「换张照片」只消费数据轨合格候选，跨事件时文字和链接一起换。
- 「读读这一天」到真实事件；顶部「记忆」「张年」使用现有路由。不能沿用预览里的假页面切换。
- 紧凑提醒只展示数据轨给出的有效状态；详情保留已认可的来源摘要/人物/语气/记录时间。前端不按文字关键词自行把事项删除或完成。
- 默认重点一条、最多两条；无提醒整块收起。无法读取时不能展示“全部完成”。不显示技术元数据和未审核来源原文。
- 正式首页没有「记一笔」、两句旧口号、底部月份/全部记忆链接、「首页设计预览」、额外卡片阵列和自动播放。

## 联调与验收

1. 数据轨提交首个契约前，先做可独立工作的字体与组件；接口变动通过两个 STATUS 对话，不让 Teddy 转述。
2. 修改共享呈现代码前写「开始改」，可测时写「可以测了 + SHA」。Git、整套检查、跨轨测量和部署串行；首个接口节点数据轨优先。
3. typecheck、lint、测试、生产构建通过后精确 commit/push main；不建分支/worktree，不代提交别人的文件。
4. 实际检查桌面 1440×900、1024 宽和窄屏 390/320；字体真加载、无横向溢出、照片主体与动作保留、链接/换图/查看器/提醒详情都能用；同步抽查既有记忆、张年、月页和详情。
5. 验证使用稳定版本的真实内容，不以 fixture 截图、零错误、计数或 SHA 代替页面。私有截图/证据放 `C:\Users\teddy\NianlifeOps\home-2026-09-13\page\`，不进 Git。
6. 向 Codex 报 `READY_FOR_REVIEW`：两轨集成 SHA、四项检查、各视口和交互证据、字体/来源说明、八原则结果与未验证项。
7. Codex 终审后，沿用已有私有部署授权，由你统一打包已提交 SHA、保留配置与回滚点、私有发布并复验。不重跑数据轨、不动 DNS/公网，不再向 Teddy 重复申请同范围私有部署许可。
