# HEALTH-01-R3 · 按 Codex R2 审核返修 · 执行回执（脱敏）

> 2026-09-20。执行：Claude Code。审核：Codex（**尚未开始**）。
> 依据 [`health-tracking-batch-01-r2-codex-review-2026-09-20.md`](health-tracking-batch-01-r2-codex-review-2026-09-20.md) 的 R2-A…R2-E。
> 前三份回执（[首次](health-tracking-batch-01-report-2026-09-20.md)、[R1](health-tracking-batch-01-r1-report-2026-09-20.md)、[R2](health-tracking-batch-01-r2-report-2026-09-20.md)）保留为历史，未覆盖。
> 本文脱敏：不含病历原文、药物剂量数值、患者标识、家庭聊天原文、汇总稿正文与私有外链。
> **本文是执行者自检结果，不是审核结论，不是医学验证完成，医学分析未经任何医生审阅。**

| 项 | 值 |
|---|---|
| 基线 SHA | `b777804` |
| 交付 SHA | 见本次提交 |
| 前序交付 | `e32cee5`、`0c99b88`、`8492120`，均保留在历史中，未回退 |
| 仓库修改路径 | 本回执（新增）、`v2/scripts/health-audit/wechat-export-dedupe.mjs`、`v2/test/health-audit-wechat-dedupe.test.mjs` |
| R3 私有目录 | `…\health-tracking\2026-09-20\run-20260920-r3\` |
| 保留目录 | `run-20260920-a`、`-r1`、`-r2`、`codex-review-r1`、`codex-review-r2` 均**原样未动** |

---

## R2-A 未读范围 —— 943 条已全部逐条读完

**做法**：把 574 条待审与 369 条「仅模型预分类即排除」合成一份 943 条阅读包，
按时间排序、带相邻上下文，由本会话**逐条阅读并登记结论**，分八批落盘，合并时校验
编号连续、无重复、无缺口、依据非空。**本轮没有再调用任何预分类服务。**

**结论分布**：纳入 238 / 相关但非已发生事实 247 / 他人主体 198 / 无健康内容 254 / **读后仍不确定 6**。

**直接印证了审核意见**：模型预分类**已排除但实际相关的有 29 条**；
反向（预分类留作候选、实际无关）112 条。那 29 条里包含**一整段此前从未登记的发热病程**。

**覆盖口径按审核卡重算**：媒体合计 1,043、非媒体 1,034，R2 读过 91，R3 读完其余 943。
→ **原候选集合内已无未读条目。**

### 但本轮发现了一个更大的缺口，如实报告

逐条阅读时，我在**上下文窗口**里读到了若干候选表里根本没有的事实（出生体重、首次排便排尿、出牙）。
据此扩展关键词重扫，**多出 597 条从来没进过候选表的消息**。

- 这 597 条**已正文级全部过目**，实质发现 51 条已入事实层，其余按可复核的规则归为「读后无新增事实」。
- **但它们只做了正文级阅读，未逐条补拉上下文**，这是本轮最大的残余。
- 更要紧的是：**扩 60 个词就多捞出 597 条，说明召回是开放的，不能宣称已穷尽。**
  R2「未读 574」与本轮「已读完 943」都只是**相对于当时那张关键词表**成立。

## R2-B 语义与时间 —— 两条改判，时间字段分离

- 审核点名的两条已改判：2026-07-27「把配好剂量的药送到托班请老师喂」＋老师回「收到」
  = **交接与接收确认**；2026-07-29「提醒摇匀」= **提醒**。均非已给药，当日无执行反馈。
- 全部微信事实分离 `message_time` 与 `occurred_at`，**未知发生时间一律保持 null**，
  不用发送时间冒充；检查器 V7 对此有反例把关。
- 逐条阅读中另外识别出多条「指令/计划/备药」被误当成给药的模式，一并按同一口径登记。

## R2-C 事实、病程、统计与医学文件同步

- 病程从**同一份终态事实**重算，`supersedes` / `superseded_by` **双向闭合**，
  **只有 canonical 行参与计数**（旧 9 行保留但不计数）。检查器 V9 有反例：
  把被替代的旧行重标 canonical 必须失败。
- **发现并修复一条真缺陷**：更正 C-009 标记为 applied，但其旧表述仍作为**当前论断**
  留在医学问题清单里——正是审核指出的那一处。已改，并新增检查器 V11：
  文档级更正的旧表述不得作为当前论断残留、新表述必须出现（更正历史行豁免）。
- 病程仍**未与医院就诊联结**，`occurred_at` 绝大多数仍为 null，如实登记为残余。

## R2-D 验证 —— 反例真断言失败

R2 的「10/10」里有一条只比较数字变化、从未断言检查失败，审核的判断成立。本轮：

- 每条反例都**断言同一个检查器返回 `False`**；构造不出反例即报「构造失败」，不算生效。
- **独立输出目录** `validation-r3/`，不重跑任何构建脚本、不覆盖已交付产物。
- 绑定终态快照：记录参与检查的每个文件的 SHA-256。

**结果：正例 11/11 通过，反例 21/21 生效，退出码 0。**
反例覆盖审核点名的六类：预约改就诊、指向不存在的观察/就诊/文书、删空规范事实、
保留更正标记但把值改错、更正回退、以及新增的旧病程重复计数、发送时间冒充发生时间、
阅读结论缺口、文档级更正回潮。

## R2-E 医学文件按终态事实逐条修订

1. **紧迫性不再自行下结论**：中危档原写「不是急诊」，已改为「**这一档不代表不是急诊**」，
   并注明 NG143 1.3.4 把面诊紧迫程度交给评估者结合整体状况判断；分档明确**可升级不可降级**。
2. **撤回「可以直接照着用」**：改为「用来判断多快去，不是用来决定不用去」。
3. **撤回「已达到反复呼吸道感染线」**：该依据本轮只有检索摘要，计数窗口与合并规则均未核实，
   现只给区间并写明效力边界。
4. **ABRS 三种模式按原指南逐字更正**：R2 把「脓涕**或**面痛」误记为「与」、
   把「约 5~6 天并开始好转后再次加重」误记为「好转 4~7 天后」，均已改；
   问题清单里「只有 >10 天一种门槛」同步补为三种模式，并接上本批按天登记的脓涕起止日期。
5. 如实记录所读指南官方入口当前标注为 ARCHIVED，不追逐版本。
6. 四份文件新增 R3 节，把本轮新事实接进去（见下）。

## 本轮改变判断的发现

1. **生长序列 16 条**——此前事实层为 **0**。最近一次有来源的测量是 **2026-05-26**（托育机构）。
   **这条直接缩短了家庭问题清单**：不必再让家里在两个历史体重之间裁决。
2. **一整段此前从未登记的发热病程**（2025-11），家长明确决定不就医、沿用上次的药，全程无原件。
3. **2026-07 病程起点前移两周**：R2 记的是就诊日，微信里的起病日更早。
4. **实际用药依从性的第一手证据**：一次就诊后的两种药「勉强吃满三天」「好像没怎么吃」；
   另一次**自行停药后又自行复用**上次开的处方药。这是本批第一次拿到「处方 ≠ 实际服用」的硬证据。
5. **2025-08 那段的服法来源是 AI 咨询，不是医生**（同段对话可证），
   且家长把 AI 给的用药阈值**自行上调**。此前叙述未区分这一点。
6. **一条跨度一年、无书面诊断的足趾问题线**，四个时间点指向同一部位。
7. **接种与出牙此前完全没有**：一次两针含卡介苗、**接种处破溃**、两次因病推迟接种。
8. **三类暴露此前一条都没有**：照护者患急性胃肠炎期间仍在带娃、孩子在未清理的宠物呕吐物上爬行、家长确诊肺炎。
9. **一条无效推断**：家长用**自己的**血检结果推断孩子没问题。原话保留并标注无效，
   既不当阴性证据也不删除。
10. **一处危险误读**（R2 已排除，本轮复核仍成立）：托班消息里的高温读数是**药浴水温**不是体温。

## 来源闭合（审核指出「拿纯函数测试替代真实来源闭合」）

- 候选**可由真实来源完整重放**：旧有新无 0 条、同 ID 正文漂移 0 条；
  重放多出的部分**全部早于出生日**，属扫描窗口的显式边界。
- 非规范输入的导出逐条核算：窗口内不在规范输入里的消息分为**渲染差异**与**真正缺失**两类，
  真正缺失的全部是媒体路径、撤回提示、群系统事件、占位符——**规范输入没有丢任何实质消息**。
- **仓库去重模块补上真实 Markdown 解析**（`parseMarkdownExport` / `unescapeMarkdown`），
  私有盘点脚本改为**直接调用仓库模块**，解析与去重不再各自漂移；
  改用后选择结果与改前逐字一致。新增 5 个合成测试，含「不得编造 ID」
  与「转义未还原会把同一条消息判成两条」。仓库测试 **22 → 27**。

---

## 检查结果（命令与退出码）

| 命令 | 退出码 |
|---|---|
| `npx eslint scripts/health-audit/ test/health-audit-wechat-dedupe.test.mjs` | 0 |
| `npm run typecheck` | 0 |
| `node --test test/health-audit-wechat-dedupe.test.mjs` | 0（**27/27**） |
| `npm test`（全仓） | 0（**1,497 项，1,486 通过，0 失败，11 跳过**） |
| `npm run build`（生产构建） | 0 |
| 私有 `r3_validate.py` | **0**（正例 11/11，反例 **21/21 生效**） |

## 外部服务与数据流向（单独披露）

- 微信候选的主体预分类由 R1 的既有脚本生成，该脚本**会把候选消息与上下文提交到已配置的
  DeepSeek API**。准确表述是「**不进 Git**」，**不是「未离开本机」**。
- 授权依据：项目 `CLAUDE.md` 长期规则与「外部 API 费用不再需要确认（2026-09-12）」。
- 调用量：R1 一次性分类 2,077 条（每条 1 次请求，含 3 次能力门探针）；医院图 OCR 亦在 R1 完成。
  **R2、R3 均未重新调用该服务。**
- 费用：脚本未记单价，**无法给出金额**，只能提供请求计数。
- **R3 本轮没有向任何外部模型服务发送数据。** 943 条与 597 条**全部由本会话直接阅读**，
  未经任何预分类；新增的只有公开医学资料检索，使用去标识化临床问法。

## 未验证项

1. **597 条召回缺口只做了正文级阅读**，未逐条补拉上下文——本轮最大残余。
2. **关键词召回是开放的**，不能宣称穷尽（见 R2-A 小节）。
3. 病程仍未与医院就诊联结；绝大多数事实的真实发生时间仍未知。
4. 实际用药新增 3 条硬证据，其余病程仍未确认。
5. 6 张无日期病历的日期为推定；35 张图**单人核对，未经第二人复核、未经医生复核**。
6. 汇总稿上游资料**仍全部未提供**。
7. 医学证据 7 条中仍有 3 条只到检索摘要级。
8. **2026-09-19 之后无任何记录**——任何「现在是否安全」的判断都不能由本批资料回答。

## 阻塞

无硬阻塞。

## 下一步

**提交 Codex 审核，状态「已提交审核」。未进入 HEALTH-02。**

## 哪里需要 Teddy

需要家庭回答的由 6 条**减到 5 条**，且其中两条改了问法：

- **删除**「历史体重用哪个」——本轮已重建完整生长序列，不需要家里裁决；
  改为只问「最近一次测量之后还称过吗」。
- **改问法**「药吃完了吗」→「**实际吃了几次、有没有漏**」——因为已查到两次未按处方服完。
- **改问法**「烧了几天」→「**从哪天开始算**」。
- 其余：最近一次复诊后的症状变化；精神/进食/睡眠/呼吸/喝水尿量。

另有三件**不用问家里、但要带去问医生**的：跨度一年的足趾问题、
与用药同期进行的非医疗干预、以及某段用药方案的实际来源。

## 边界遵守声明

未连接生产或 Neon、未写业务数据库、未跑迁移、未启动导入 worker、未改页面、未部署、
未回退 SHA、未新建分支、未强推、未改写含真实群标识的旧历史、
未修改其他会话的工作区改动，也未改动既有运行目录与两个审核目录。
原始资料只读，运行前后哈希一致。
健康原文、原图、派生放大图、患者标识、家庭消息与汇总稿全文**全部留在私有目录，未进 Git**。
