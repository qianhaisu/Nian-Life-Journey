# HEALTH-02：仅来源绑定收尾（已取消本机 Postgres）

日期：2026-09-21。最新覆盖：Teddy 表示太慢，明确取消本机 PostgreSQL 安装。本卡原 B 阶段的下载、初始化、Postgres 存储实现和集成测试全部撤出本轮；旧版本要求不再执行。此前两项绑定收尾的授权仍有效。

状态：卡已更新，待原 Claude 会话 ACK；不表示已通知执行者。如果已经开始原 B 阶段，停止继续安装/开发，先报告实际进度与本任务启动的进程；仅停止本任务进程，不卸载或清理既有环境、不删除未提交工作。保留已做成果，避免额外返工。

## 本轮只做两项

读取 `docs/health-tracking-batch-02-r1-fix-codex-review-2026-09-21.md` 的两个残留缺陷；独立反例为 `C:/Users/teddy/NianlifeOps/health-tracking/2026-09-21/codex-review-health02-r1-fix/binding-probes.mjs`。

1. 旧事实版本不得展示后来才新增的来源；bindingAt 为 null 不回退最新证据。当前关系与历史审计分开，后来撤销/修订关系不能污染或抹去旧依据。
2. 待复核绑定持久保留，重放仍可见；相同事实正文可补交明确来源版本完成确认，不强迫改正文。保留确认人、时间、理由和前后依据，确认重试幂等，冲突不静默覆盖，旧版本绑定保持可追溯。

保留已通过的 52 项和第一批结果，不扩词、不重新整理病例、不扩充产品功能。先 ACK 当前 main 基线、拟改路径与本轮 ETA，然后实施；不建分支、不新建会话、不自动调度。

## 验收与交付

- 仅允许健康领域绑定/追溯/待决状态相关模块、必要合成测试及脱敏回执改动；保护其他会话修改。
- 两项独立反例加入合成回归，覆盖新增来源的历史追溯、重放、明确确认、冲突确认及历史不变。复跑受影响健康测试和项目规定 lint/typecheck/全仓测试/build，构建不连库。
- 私有产物另存 `C:/Users/teddy/NianlifeOps/health-tracking/2026-09-21/health-02-binding-fix/`；旧审核、运行目录只读。真实资料仅文件级核对，保留输入哈希、记录守恒和全量/缓存一致性证据，不重做既有医学阅读。
- 回执 `docs/health-tracking-batch-02-binding-fix-report-2026-09-21.md`，列实际 SHA、两项反例结果、复跑命令和限制。精确提交并 push 当前 main，不夹带其他工作。
- 交付后等待 Codex 独立审核，不自行进入 HEALTH-03/04，不部署。

## 数据库边界

本轮不下载/安装/启动本机 PostgreSQL，不实现新的 PG 存储，不跑 SQL 迁移或真实库测试，也不改用 RDS。现有文件账本的并发测试结论继续限定于文件账本。

真实 Postgres 事务/并发保留为后续数据库接入前的必验项，不作为本轮来源绑定收尾的追加阻塞。HEALTH-02 若其余门槛通过，只能按已交付的文件账本范围验收；不宣称数据库通路或 RDS 已验证。实际业务库接入、迁移与部署仍需另行授权和验收。
