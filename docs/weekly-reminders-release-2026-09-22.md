# 每周提醒修复发布记录 — 2026-09-22

用户在本轮明确授权 Codex 直接修改并发布，不转交 Claude Code。

- 生产：https://nianlife.cn/
- 应用 SHA：`91f8417ceed299bc77e8cc0e14f8933500179bfa`
- 切换：2026-09-22 12:56（北京时间），容器 healthy，公网健康检查 SHA 一致。
- 原线上：`ad75b78`；本次没有数据库写入或迁移，没有修改家庭内容。

## 修复

原实现每天滚动显示“过去七天”，9/22 的窗口变为 9/16–9/22，导致 9/15 提出的未结束计划退出首页。
现按上海日历的周一至周日显示，本周为 9/21–9/27。无明确日期且仍有效的计划继续展示；有日期的日程按与本周是否相交判断；同级事项优先显示最近提及者。原有保鲜、取消、取代、来源审核和勾选规则保留。重要已完成事实按完成证据日归周。

另同步三个过时的夜间编辑测试：既有 e509e17 已将 READY_LAG_DAYS 从两天改为一天，但测试未更新。本轮只更新测试断言，不改夜间编辑逻辑。

## 验证

- 类型检查、生产构建通过；服务器 Docker 镜像构建通过。
- Lint 通过；排除本地 `.next-*`、`.data`、`.npm-cache`、`.vercel` 生成目录，保留源码检查。
- 全量测试最终复跑：1598 项，1587 通过、11 跳过、0 失败。
- 公网 Chromium：1365×900、390×844；日期均为 2026-09-21 / 2026-09-27，展开后 6 条提醒，目标未结束计划存在，无横向溢出、无 pageerror。
- 当前可见事项中的 1 条勾选与服务端一致；数据库共 2 条勾选保留。浏览器检查只读，没有模拟勾选写入。
- 发布前后 upcoming_items：32 行，完整行聚合校验值相同。
- 发布前后 upcoming_checks：2 行，完整行聚合校验值相同。
- 健康接口：ok=true，数据库 connected，OSS reachable。
- 未实测 iOS / 微信内置浏览器；手机宽度检查使用 Chromium。

## 回滚与清理

统一使用 `v2/scripts/deploy-ecs-public.sh upload/build/swap`，沿用当前生产运行配置与三个内容/健康目录挂载，健康手记保持单写者。

保留当前应用及最近两版回滚：

| 类型 | 镜像 SHA | 容器 |
|---|---|---|
| 当前 | 91f8417 | nianlife-diag-web |
| 最近回滚 | ad75b78 | nianlife-diag-web-pre-91f8417-20260922-125601 |
| 第二回滚 | 4ca7fe2 | nianlife-diag-web-pre-ad75b78-20260922-094357 |

成功切换后 ecs-retention.py 自动清理完成：移除 1 个更早停止容器、旧应用镜像 80fc748，以及无引用镜像/构建缓存；释放约 2.36 GB。未删除数据卷、内容或运行配置。

磁盘：总量 40G、已用 9.6G、可用 28G（df -h）。

回滚入口：`deploy-ecs-public.sh rollback-app nianlife-diag-web-pre-91f8417-20260922-125601`（使用现有 ECS 连接环境变量）。
