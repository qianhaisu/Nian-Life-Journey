# HEALTH-04-R1 定点复审

审查 `a3c6ebc`，已核对 origin/main。结论：**多数修复成立；仅以下两组边界补正后再收口。不再改 UI，不重开历史全量阅读。**

## 已成立部分

- 独立复跑 `test/health-page.test.mjs`：10/10通过，约0.83秒。本人未挂靠节点、手记睡眠/鼻音、无就诊号医院事实入口均有实现及测试。
- 区间支持记录更正后的标识已传到经过、总结和关联措施；配置有效时来源文件变更会触发待核。
- 恢复说法作为待定反证保留、入托依据绑定派生记录指纹的实现已读。C项本轮不再追加返修；这里确认的是派生记录版本校验，不等同于自动监测原微信导出文件的所有变化。
- 浏览器39项、57输入哈希及工程全套结果为执行方证据，本轮未重复跑；编辑器既有3项失败仍单列，不要求健康执行方修改。
- 独立合成探针和结果：`C:/Users/teddy/NianlifeOps/health-tracking/2026-09-21/codex-review-health04-r1/probes.mjs`、`results.json`。只使用合成fixture，不改真实资料。

## 1. 旧引用不能绕过当前身份与角色

`model.ts` 计算 exclusionReason 后，仍用 `inReview.has(id)` 绕过排除。区间支持/反证ID即使在更正后明确属于别人，仍会被当成孩子的时间轴节点。

独立探针把 O5 的 subject 更正为 other_child、role 更正为 question：分类器输出“主体是别人”，区间已 needs_review，但 `stillShown=true`。当前仅 attribution=not_child 有提前硬拦截，其他主体/角色更正没有同样保护。

修正：当前明确的非本人身份和非事实角色优先于旧审核引用；旧引用可留在历史待核依据里，不能重新生成孩子的事实节点。核对其他显式排除字段采用同一优先级。只补这一类身份/角色更正反例，保留已修好的未挂靠本人可见。

## 2. 失效不能只依赖红色区间，也不能把未核验当 current

### 2a 医院事实更正

当前总结与措施主要依据 pendingBands 判断失效。独立探针更正 EP-X 所关联医院诊断 CF1 后，当前就医记录读取了新值，但 summary.points 仍写旧诊断“急性支气管炎”，summary.review=null，关联措施仍 current。这仍是上轮要求修复的“历史依据更正后旧论断继续有效”，不是新增功能。

修正：对总结/措施依赖的医院事实、病程及来源建立版本核对；可复用 HEALTH-02 现有依赖能力。暂时没有细粒度绑定时，保守标记受影响病程待核即可，不要求自动生成新医学解释。不能仅以区间是否失效作为全部历史更正的代理。

### 2b 来源核验未配置

服务层没有 HEALTH_PAGE_MATERIAL_ROOT 时不提供 materialSourceHash，模型因此跳过来源核验并仍标 current。使用正常64位来源哈希、实际无法读取源文件的合成输入，独立结果 `missingSourceRootStatus=current`。

修正：材料存在而核验配置缺失、不可访问或哈希格式无效时，明确待核/不可用，不伪装已核验。去掉生产代码里 `sha256 !== "0"` 的跳过判断，测试也使用有效合成哈希。需要区分“通用护理可保留展示”与“来源已确认有效”；不必把通用护理全部撤下。

## 交付

只修以上两组，补对应合成反例，复跑页面目标测试和必要集中工程检查；不扩成新一轮设计/资料普查，不重复此前已成立的全部验证。

回执 `docs/health-tracking-batch-04-r1-fix-report-2026-09-21.md`，新私有目录 `health-04-r1-fix/`。保留旧产物和其他会话修改，精确提交并 push 当前main。不接库、不部署、不调外部模型、不进入下一批。完成状态“HEALTH-04-R1 定点补正已提交审核”。
