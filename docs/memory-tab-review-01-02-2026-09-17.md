# MEMORY-01 / MEMORY-02 总审核与下一轮修订提示词

日期：2026-09-17。审核基线：`main @ 8e033e4`。

本文件是 Codex 对两份 Claude Code 交付的审核及下一轮交接，**不是生产变更或新自动任务的执行回执**。本轮只读报告、相关代码、HTML 源文件和已有原图/裁切图；未连接生产库、未运行 Organizer、未调用识图模型。原报告由其作者保留，不由 Codex 覆写。

## 1. 结论

| 任务 | 审核状态 | 判断 |
| --- | --- | --- |
| MEMORY-01 | 诊断方向保留，补证后验收 | 导入之后缺少持续整理是合理方向；9/13 页面 bug、缓存排除、上海日期统计及单条记录追踪尚未证实完整 |
| MEMORY-02 | changes_requested：修订一次样稿 | 一条日时间线、同日故事合并、统一资料展开的方向保留；封面、月总结、发言人和关键样例不满足交付标准 |
| MEMORY-03～08 | 尚未放行整体实施 | 先补这一轮样稿和必要核查；最终设计仍由 Teddy 看完整实际效果后确认 |

无需让 Teddy 再决定“阿爷要不要查”“截图现在还是以后处理”“9/13 要不要插队”。这些可直接归入既有步骤；真正需要他判断的是修正后的阅读效果。

## 2. MEMORY-01：保留什么，补什么

输入：`C:\Users\teddy\NianlifeOps\memory-tab-20260917\01-freshness\MEMORY-01-report.md`。

### 保留的诊断方向

- 既有导入日志证明 23:30 时段导入运行过，随后整理/月回顾/刷新被明确跳过。不能继续把“脚本返回 0”当成记忆页已更新。
- 根据执行方报告，生产查询目标已核对为 RDS，Neon 是保留的旧连接。后续必须使用已核验的生产目标；不要为了脚本能跑把旧 Neon 凭据重新命名为 `DATABASE_URL`。
- 不重复修复已有的 `.cmd` 编码问题，不重新全量导入，不直接删除三个 skip 参数启动生产写入。
- 文档中涉及旧数据库的说明应精确更正，但当前 `CLAUDE.md` 有其他会话未提交改动，先由原写者协调该段；不得整体覆盖。RDS/OSS 的主架构也不能证明全部历史媒体都已离开 R2。

### 必须补的四项证据

1. **9/13 是缺失现象，尚不能断言页面算法有 bug。** 报告把 `family` 和泛化的 `approved` 当成日照片准入；但实际读取层还要验证媒体可交付、最新有效 `media_subject_check`、照片尺寸与连拍代表。对 64 张候选按相同口径给出各层数量及排除原因，再与 9/5、9/6、9/10 比较。9/13 未核验的封面候选本身已说明 `media_topic` 通过不等于主体通过。
2. **分清默认阅读与折叠部分。** 分别报告这一天是否进入 `chapter / chronicle / archiveDays / archiveDaysVisible / 展开接口`。初始 HTML 没日期，不足以证明整个读取链路丢了日期。新需求要求统一时间线，属于后续产品改动；现有代码是否漏选则须另行证明。
3. **缓存结论收窄，补一条真正可追踪的记录。** “页面显示 9/14”无法排除缓存。先写“缓存因素未排除”，必要时补同一运行版本的即时编排结果与页面对照。当前报告追踪的是会话内 60 条聚合数据；应选择一个稳定的 source ID，列 source → media asset → media → 最新有效审核 → 事件关联 → 页面位置，保留可重复的只读探针与脱敏结果。
4. **统一上海日界和查询范围。** 修正“09-15 即 bucket 09-14”，以及“统计到 9/16”同时又“未纳入 9/16”的矛盾。明确字段类型、`captured_at` 与 `taken_at` 的口径、窗口边界；重新列 9/13、9/15、9/16。近五天队列为零不能外推为从未运行；定时自然触发也须有调度事件证据，否则只写该时段程序运行证据。

相关代码证据：

- `C:\Users\teddy\Documents\Nianlife\v2\lib\family-archive.ts:119`：可见性与媒体交付条件。
- `C:\Users\teddy\Documents\Nianlife\v2\lib\media\story-binding.ts:171`：主体审核的最新有效决定。
- `C:\Users\teddy\Documents\Nianlife\v2\lib\publication-moments.ts:282`：主体筛选后选连拍代表和合格主图。
- `C:\Users\teddy\Documents\Nianlife\v2\lib\publication-moments.ts:466`：主体 store_only 的排除。
- `C:\Users\teddy\Documents\Nianlife\v2\lib\publication-moments.ts:615`、`:692`：quiet day 与相册初始展示分层。

## 3. MEMORY-02：需要返工的具体问题

输入：`C:\Users\teddy\NianlifeOps\memory-tab-20260917\02-sample\MEMORY-02-report.md` 和同目录 `mockup.html`。

### A. 封面不能按当前推荐直接采用

- 报告第 27–29 行同时列出主体 approved 与 `store_only / hold:duplicate-photo`，却直接说“不影响可用性”。须拿完整 media ID、审核类型、审核时间、审核来源与当前有效决定解释；不能自行忽略重复保留决定。
- 推荐封面文件与日故事中的奔跑照不是同一张，报告混淆了画面描述和推荐理由。必须核对文件 → media ID → 日期 → 场景的映射。
- 已查看手机裁切 `covers/sep07-smile-a-crop-mobile.jpg`：下颌被下边缘切掉；桌面版本更完整。沿用原来的 `50% 30%` 并不能证明构图已改好。应逐图调整焦点、画框比例或换图，并交实际完整月份卡片的两端截图。

### B. 月总结仍然没有完成编辑目标

- 新稿仍按“语言、点心、画画、爬床”逐项串接，主要是把原快照接成一段，概括性不足。
- 出现“永远”“喜欢”“回家”“跟着某人学舌”“整觉的信号”等扩大频率、地点、行为或因果的表达，不能仅因原事实已在生产就认为这些新表达有依据。
- 报告声称两段，实际 HTML 第 121 行是一个 `<p>`。应围绕最多两个有证据的当月变化组织自然段，每个主题只用少量细节支撑。
- 月份一句话的语言主题可以保留为候选，但其独特性和温度还要结合完整九月材料审核，不以“跨两天”自动判为最佳文案。

### C. 先把发言人核对一致

HTML 自身已有冲突：第 202 行正文与第 208 行资料区对同一句回复、同一条精力描述归属不同角色；第 215 行和第 223 行对 cold/hot 的发言人也不一致。

应逐条建立 source ID → 原始 sender → 确认后的显示名/角色 → 正文引用的映射。既有故事可能本来就归属错误，不能一边声称“现状已合格无需改”，一边列出与它冲突的资料。原始名字如有歧义，保留真实显示名，不猜角色。

9/8 合并为一篇、早晚分段的方向保留；“同一天”不等于“同一事件”。次数、洗澡前后、意图和多人情绪等细节须有具体来源，否则删去修辞性补充。仅凭一张照片也不能确定先后过程。

### D. 关键交互与验收样本未完成

- 用户指定的无图 Event `/events/event-v2-18f883d31810d166ee4391eef701c497`（9/1）尚未进入样稿；必须补它，不能只展示 9/7 已有图的对照和 9/8 合并示意。
- Event 要默认显示全部“最终精选补图”；月页首屏 1–3 张不能变成 Event 的总数上限。绑定图与同日精选去重，仍保留“同日记录”与“故事现场”的语义差别。
- HTML 第 169、179 行的“展开”是说明文字 `<div>`，还指向下方 Event 说明，没有演示用户要求的原位展开/收起。补一个可实际操作的同日精选区。
- “20 条资料”目前只显示若干压缩片段加省略号；`6+5+2+7` 相加不证明 source ID 并集已经去重。一次展开后按时间逐条完整显示，准确标注数量、发言人、日期及上海时间。
- 9/5 视频封面目前作为普通照片显示，须明确视频身份和播放入口；具体全屏实现留 MEMORY-06。
- 样稿遗漏生产中文字体 `NianRound`，不可声明已经完整沿用现有字体。完整样稿应复用实际字体资源及阅读尺度，避免用一页密集报告替代三层产品页面。

本轮浏览器工具拒绝打开本地 `file:` HTML，未尝试绕过；因此上述 HTML 结论是源文件审查，图片结论是已有图片的直接观察，**不算手机/桌面完整页面已验收**。修订作者需补可直接审阅的页面截图及交互证据。

## 4. 对三个开放问题的处理意见

| 问题 | 处理意见 | 实施位置 |
| --- | --- | --- |
| 原始聊天中的语言片段尚未进 Organizer | 纳入带来源的待审核候选；是否存在 life_event 是处理状态，不是真实性判断。不得额外添加全家情绪、第一次等断言 | MEMORY-04；本轮先补 source ID 与上下文 |
| 9/6 网页截图混入当天媒体 | 已看原图确认它们是旧页面截图。应排除于“当日生活照片”的精选候选，保留原始资料；不需要再决定现在还是以后处理 | MEMORY-03 第一项，作为固定反例；不把 media_topic 当主体放行 |
| 9/13 优秀候选尚无主体审核 | 纳入九月正常主体核验清单；通过当前有效审核后才参与封面比较，不设“高分自动放行” | MEMORY-03；本轮先查准入账本，不临时改生产状态 |

Organizer 分两件事安排：

1. **九月追赶**：MEMORY-04 由既有 Claude 数据执行者手动、有界处理明确缺失的日期/来源，先生成候选，按既有审核流程整理成篇；纯照片日不依赖先造一个故事才能显示。
2. **持续日更**：在 MEMORY-07 验证整条链路时明确“谁触发、处理哪些增量、谁审核、何时刷新、失败如何留证”。不能让技术运行成功直接等于内容获准发布。此处明确分工，不创建自动任务或启动现有生产写入。

## 5. 给 Claude Code 的下一轮完整提示词

```text
执行 MEMORY-01/02-R1：只补证据和修订九月样稿，不开始业务实现，不改生产数据或后台开关。

读取：
C:\Users\teddy\Documents\Nianlife\docs\memory-tab-implementation-plan-2026-09-17.md
C:\Users\teddy\Documents\Nianlife\docs\memory-tab-review-01-02-2026-09-17.md
以及 MEMORY-01、MEMORY-02 既有报告。当前用户指令优先，不使用旧 Cowork/分支约定。

先 ACK 并记录当前 main SHA。保留所有其他会话未提交改动，不新建分支。
不重新导入、不运行 Organizer、不调用新模型、不写审核状态、不部署。
必要的生产补证仅复用已授权的只读入口，先核验目标身份；不打印连接串、凭据或完整私人原文到 Git。

A. 修正 MEMORY-01 的过度结论：
对 9/13 的候选列出 family → 可交付 → 最新有效主体批准 → 尺寸/连拍 → 各展示层的数量和原因。
与 9/5、9/6、9/10 同口径比较，确定审核缺口还是页面漏选；不得为证明页面 bug 删除现有门槛。
统一上海日期与窗口，补 9/13、9/15、9/16 的可核验结果；避免 bucket 差一天和范围自相矛盾。
用一条稳定 source ID 完成端到端追踪，保留脱敏查询和结果；缓存不能排除就明确写未排除。
把“近五天未运行”与“从未运行”、程序运行与自然定时触发分别表述，不用推断冒充证据。

B. 修正 MEMORY-02 样稿：
核对封面文件与 media ID，查清主体 approved / duplicate store_only 的有效决定及保留代表图。
逐图重做裁切，手机不切下巴，桌面保留关键动作；提交完整月份卡片，不只交小裁切图。
月总结围绕最多两个有证据的变化主题重写，删去绝对化、习惯化、地点和因果补写，实际用自然段呈现。
核对正文与原始资料的发言人；每个关键句都有 source ID 与原文支持，已有故事也不能免审。
9/8 保留同日一篇、早晚自然分段；不把同日全部碎片认作同一事件，不补没有证据的次数/动作先后。
补用户点名的 9/1 无图 Event，展示全部已选的合格同日补图，不能套用月页首屏张数上限。
至少一个“展开/收起当天精选”可实际操作；Event 原始资料一键完整展开，一条来源一行，数量按 ID 去重。
视频封面标明视频与播放入口。沿用实际生产字体，产品样稿与审核说明分开。
9/6 网站截图排除于样稿精选，保留原文件；9/13 未审图保留待核验状态；语言新线索保留待审候选。

输出到原步骤目录，使用新文件名 MEMORY-01-R1-report.md、MEMORY-02-R1-report.md、mockup-r1.html，
保留原版。附手机/桌面完整截图、代表交互证据、选图 ID 清单和逐项修正对照。
私人资料和图像只留 NianlifeOps 本机目录；仓库只提交脱敏的短回执，文档检查后自动 push main。
交付后由 Codex 复核，再把修正后的实际样稿交 Teddy 确认，不自动推进 MEMORY-03～08。
```

本轮 Codex 仅形成这份审核/修订交接；未唤醒 Claude、未代替 Teddy 批准设计、未执行生产修复。
