# HOME-20260913-PAGE · 页面 Claude Code 状态

> 本文件由页面 Claude Code 独占维护。数据轨状态见 `docs/HOME-DATA-STATUS.md`。

## ACK（真实接单）

- 任务 ID：HOME-20260913-PAGE　ACK：2026-09-13 18:15 +0800
- 接单时 HEAD：`74e76861f815f0cebf77262db7f1dafdb444a7f2`（派单卡记的 `ac90ec6` 已被取代，未回退 checkout）
- 已核对唯一视觉基准：`...\01a099d7-35aa-7531-83c8-39abce4014d9\nianlife-home-v2.html`，实测 SHA-256
  `9a046b3b5a4ee873375a27f3b969ebfedb72ddee59d7a41f4b3475ac90f755cc` = 规格记载值
- 工作区既有未提交改动（非本轮，不动、不代提交）：`docs/HANDOFF-COMMANDER.md`、`docs/ORCHESTRATOR-INBOX.md`、
  `docs/nianlife-handoff-2026-09-06-neon.md`、`v2/scripts/quark-heic-ingest-linux.mjs`

## 已交付（两个提交已 push）

**1. 中文圆体自托管（真字形，不是系统回退）**

- 源：`chill-round.ttf`（ChillRoundF v3.000，OFL，14,260 码位 / 12,093 基本区汉字），仓库外，不提交。
- 生成脚本 `v2/scripts/build-round-font.py`：按 OFL 第 3 条把 name 表 1/3/4/6/16 改成 **NianRound**（不沿用保留字体名），
  许可证原文 + 修改说明放 `v2/public/fonts/nian-round/LICENSE.txt`。
- 两层分片共 37 片 3.3 MB：`cover-NN` 按码位覆盖全部汉字与中文标点（连续区间，CSS 只占几十字节）；
  `text-NN` 是按仓库中文语料词频 + GB2312 一级字排的常用字，写在 CSS 后面因而优先命中。
  `v2/app/fonts.css` 34 KB（若逐码位写全部覆盖层则是 77 KB）。**未来生僻字有覆盖，不掉回系统字体。**
- 站点字体栈：`Nunito, 'NianRound', PingFang SC…`——英文数字仍走 Nunito，汉字落圆体（`globals.css` 两行）。
- 实测（本地 Chrome，真渲染）：`document.fonts` 里 7 片 `NianRound` 状态 loaded；放大首屏标题可见圆头笔画与实心圆句号。

**2. 首页（`v2/app/page.tsx` 已切到定稿版式）**

- 结构：今天 + 今天几岁 → 一张照片和它所属的那段真实生活（换张照片 / 读读这一天）→ 至多一条近况 → 紧凑提醒。
- **已删**：底部月份与「全部记忆」入口（用户明确追加）、原首页的近况概览 / 上月回顾 / 完整待办清单 /
  同日条目 / 最近照片组 / 忽然想起。内容一条没删，各回记忆页与月页。没有「记一笔」、没有「首页设计预览」。
- 新组件：`v2/components/home-lead.tsx`（客户端：换图 + 查看器）、`v2/components/home-reminders.tsx`（`<details>`，无额外 JS）。
  作用域样式 `v2/app/home.css`。共享文件只动三处：`layout.tsx` 引入 fonts.css、`globals.css` 字体栈、
  `photo-viewer.tsx` 导出已有 `ViewerModal`（首页封面复用同一个查看器，不另造）、`upcoming-tasks.tsx` 导出三个标签常量（不重复一套文案）。
- 接的是数据轨 `home-feed/1.0.0`（`91786ad`）：`readHomeFeed()`。换图消费 `photoCandidates`（(故事, 照片) 对，
  换图必然连带换标题/日期/当时年龄/链接）；`photoAbsence` 时只留文字不画空框；`reminders.unavailable` 整块不画，
  `clear` 才写「没有要记着的事」并印出窗口起止。
- 强调色由内容驱动：只给真的带「」且不超过 10 字的引语上色（实测那段摘录有一句 27 字引语，整句上色后半段摘录全红）。
  没有硬编码的 cold/hot 模板。

**3. 本地真数据联调环境（仓库外，`.data/home-dev-rds.mjs`，不提交）**

- 发现并绕开一个真实事实：`v2/.env.local` 的 `DATABASE_URL` 指向的仍是**休眠的 Neon**，而且 `0013/0014/0015`
  三个已提交迁移没在它上面跑过（`growth_records.precision` 不存在 → 首页整张档案读 500）。
  线上私有站（`127.0.0.1:18080` 是到 ECS `47.99.243.155:3000` 的 SSH 隧道）走的是阿里云 RDS `nianlife`。
  **本轮我没有改任何数据库、没有跑迁移、没有改 `.env.local`。** 这条是给数据轨/总指挥的事实记录。
- 本地验证用单进程覆盖：ECS→RDS 隧道 + OSS 公网 endpoint（那份凭据里的是 VPC 内网地址，本机连不上，
  表现为挂 135 秒后 404）。照片因此在本地能真加载。

## 实测（本地 dev，真数据、真照片）

- 1440×900 / 1024×768 / 390×844 / 320×700 四个视口无横向溢出（`scrollWidth == clientWidth`）。
- 交互：换张照片 → 照片、标题、日期、当时年龄、`/events/<id>` 一起换（实测 9 月 7 日「他会说 cold，也会说 hot」
  ↔ 8 月 19 日「小年也扎了个小辫子」）；点封面 → 站点已有查看器，原比例 1280×959 `object-fit: contain`，
  返回键关闭；提醒展开 → 提出 / 角色 / 语气 / 记录于 / 已审核摘要 / 回到那一天。
- 触摸目标：换张照片 61×44、读读这一天 133×44、查看详情 68×44，均 ≥44。
- 窄屏修正两处：问候与「现在几岁」在 390 以下改上下排（并排会把标题折成「最近怎么 / 样，张年。」）；
  版心左右留白跟 `.header-inner` 对齐（原先品牌 x=32、照片 x=16）。

## ✅ 可以测了 · SHA `f336c10`（2026-09-13 20:5x，前一版 `eb57f5c`）

数据轨可以对**完整呈现链**做复核了。接的是 `home-feed/1.1.0` 的字段；`1.2.0`/`1.3.0` 只改了选片与
习惯上限的内部规则，页面消费的字段没变，当前树（含你们 `2798593`）typecheck / lint / 1090 条测试
（1080 通过 0 失败 10 跳过）/ 生产构建全过。

两个提交：`f789da3`（定稿首页 + 中文圆体）、`eb57f5c`（1.1.0 接线 + 冷热两色 + 字重修正）。

**唯一验收服务**（本机，仅回环，不对外）：`http://127.0.0.1:3200/`
——`next start`，PID 48500，跑的是含 `eb57f5c` 的生产构建；经 `.data/home-dev-rds.mjs` 走
ECS→RDS 隧道（15488）读真实生产数据，媒体走 OSS 公网 endpoint。
它现在是**脱离 Claude 任务树**起的（之前挂在后台任务上，任务一停服务就死，并且残留进程导致
再起时 `EADDRINUSE: :::3200`——那次「启动失败」的真正原因是重复启动，不是构建或代码问题）。

### 你（数据轨）提的那一行：已改，实测已对上

`app/page.tsx` 的证据链标签现在按 `evidenceKind` 分：`event` → 「看那一天」，`month` →
「翻到 2026 年 8 月」，`undefined` → 不画链接。生产实测 5 条全是 `month`，链接 `/memory/2026/09`、
`/memory/2026/08`，标签与去处一致。

### 本轮另外三件（Teddy 2026-09-13 当面提的）

- 超出两条的**有效**事项进「还记着的其他事」折叠；折叠里只放 active / needs_confirmation / tentative，
  **过期的不从折叠层回到首页**（`more` 里混着过期与已完成，页面侧再过滤一次）。
- 无有效提醒 → 整块不画。`unavailable`、`clear`、`ready 但 shown 为空` 三种都不画，一种都不写成「全部完成」。
- 冷热两个词两个颜色：标题里 `cold` 蓝、`hot` 桃红（定稿如此）。

### 追加 · `f336c10`：标题强调改成逐篇指定（Teddy 2026-09-13）

按关键词（cold/hot/冷/热）自动匹配着色已**删除**——那张表会自己长大，最后每个标题都自动变色。
改成通用机制：每个标题最多两段强调，片段必须是标题里**真有的**文字，颜色从站点已有的三支里挑
（蓝 `#537b95` / 桃红 `#b26450` / 鼠尾草绿 `#74866a`）；没有配置就是默认文字色，片段对不上就退回纯文字。
配置在 `app/page.tsx` 的 `TITLE_EMPHASIS`，键是 eventId，**只属于那一篇**。正文短引语规则未动。

生产构建实测：9 月 7 日那篇两段强调，色值 `rgb(83,123,149)` / `rgb(178,100,80)`；换到
「小年也扎了个小辫子」「张年入选毕业庆典节目」后标题强调 0 段、默认文字色，正文短引语照常桃红。
针对性检查：typecheck / lint / 1100 条测试（1090 通过 0 失败 10 跳过）/ 生产构建全过；
未重做整套视觉验收（上一轮四视口证据仍有效，版式未变）。

## 锁与串行

- 19:0x 持有 Git + 全量检查时段 → **20:2x 已释放**。`eb57f5c` 已 push main。
  本轨下次要用会先在这里写「申请」。
- 共享呈现代码（`layout.tsx`、`globals.css`、`photo-viewer.tsx`、`upcoming-tasks.tsx`、新组件）已改完并 push，
  现在处于稳定状态，可以扫、可以测。

## 生产构建下的页面验收（证据 `C:\Users\teddy\NianlifeOps\home-2026-09-13\page\`，不进 Git）

| 项 | 结果 |
|---|---|
| 1440×900 / 1024×768 / 390×844 / 320×700 | 四个视口 `scrollWidth == clientWidth`，无横向溢出；截图各一张 |
| 真字体 | `document.fonts` 里 8 片 `NianRound` loaded；放大可见圆头笔画与实心圆句号，不是系统回退 |
| 换张照片 | 照片 + 标题 + 日期 + 当时年龄 + `/events/<id>` 一起换（9 月 7 日「他会说 cold，也会说 hot」↔ 8 月 19 日「小年也扎了个小辫子」） |
| 查看器 | 站点已有 `ViewerModal`，原比例 1280×959 `object-fit: contain`，返回键关闭 |
| 提醒 | 默认 2 条；展开见提出/角色/语气/记录于/已审核摘要/去处；折叠入口再出 3 条有效事项 |
| 触摸目标 | 换张照片 61×44、读读这一天 133×44、查看详情 68×44 |
| 回归 | `/memory`、`2026 年 9 月`月页、`/about`、故事详情 四页正常；月页大标题的合成粗体问题已在本轮修掉 |
| 底部 | 没有月份/「全部记忆」入口，没有「首页设计预览」，没有「记一笔」 |

## 未完成 / 待办

1. `READY_FOR_REVIEW` 报 Codex（本文件即证据索引）；终审后按既有私有部署授权统一发布 ECS 并复验。
2. 私有部署尚未执行：线上 `127.0.0.1:18080`（→ ECS `47.99.243.155:3000`）跑的仍是旧构建。
3. 原则六（浮现）、原则七（月度回顾）在新首页范围外——**未交付**，见下。
4. 临时夹具 `v2/public/__viewport.html` 已删除，未进 Git。

## 产品八原则（页面侧，八条都写，未交付的照实写）

| 原则 | 结果 | 看到了什么 |
|---|---|---|
| 一 Person First | **过** | 打开首页不点任何东西：今天是 9 月 13 日、他现在 1 岁 8 个月、9 月 7 日那天他会说 cold 也会说 hot（配一张那天的照片）、9 月 4 日还记了一条、接种记录要去核对 |
| 二 Two Clocks | **过** | 今天/照片/故事/近况/提醒期限各带自己的日期与当时年龄；页面上没有裸库时间 |
| 三 Media First | **过** | 家人会读到的文字里没有工程名词、没有来源系统名、没有计数式描述（折叠入口写「还记着的其他事」，不写数字） |
| 四 Invisible Automation | **① 过　② 否（未交付）** | ① 首页没有上传主按钮、没有空状态引导上传、没有红点、没有「系统会自动整理」；② 管线仍非无人值守（数据轨同判） |
| 五 Not Equal Weight | **过** | 一张照片 + 一段故事是页面的主体，近况一行、提醒一条；其余默认不出现 |
| 六 Bring the Past Back | **未交付** | 定稿把「忽然想起」从首页去掉了，浮现回到记忆页/月页。已确认首页**没有**无条件占位模块、没有「暂无」、没有随机轮播、没有定时器 |
| 七 Automatic Reflection | **未交付** | 月度回顾不在新首页范围内，月页未改动 |
| 八 Family Owns It | **过（本轮修好的）** | 主故事、近况 → `/events/<id>`；提醒 → 已审核来源摘要（谁提的、什么语气、哪天记下的）+ 按 `evidenceKind` 正确命名的去处 |
